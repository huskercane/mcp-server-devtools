[
  {
    "name": "artifact_read",
    "description": "Read a resumable byte chunk from a temporary artifact returned by another tool. Data is base64 encoded; continue with nextOffset until eof is true.",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "artifactId"
      ],
      "properties": {
        "artifactId": {
          "description": "Opaque artifact ID returned by a tool such as `circleci_logs`.",
          "type": "string"
        },
        "offset": {
          "description": "Zero-based byte offset. Use the prior response's `nextOffset` to resume.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 0
        },
        "maxBytes": {
          "description": "Maximum bytes to return. Defaults to 64 KiB and is capped at 1 MiB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false
    }
  },
  {
    "name": "artifactory_download",
    "description": "Download one Artifactory repository item into a bounded, owner-scoped artifact. The file remains opaque and archives are never extracted. Verifies X-Checksum-Sha256 when supplied and computes SHA-256 locally. Returns metadata only; use artifact_read for bounded chunks.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "repo",
        "path"
      ],
      "properties": {
        "repo": {
          "type": "string"
        },
        "path": {
          "type": "string"
        },
        "maxBytes": {
          "description": "Maximum transferred bytes, default 32 MiB, hard maximum 512 MiB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "artifactory_get_build_info",
    "description": "Get JFrog Artifactory **build information** for one build run — the modules it produced, their published artifacts (name, type, checksums) and resolved dependencies, plus agent, VCS revision, start time, duration, and properties. Use it to answer \"what exactly did build N publish, and from which commit?\". Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/build/{buildName}/{buildNumber}`. The response is wrapped in `buildInfo` (with `name`, `number`, `started`, `durationMillis`, `agent`, `buildAgent`, `vcs[]`, `modules[]`, `properties`) and `uri`.\n\nAuthenticates with a JFrog **access token** (`ARTIFACTORY_TOKEN`) sent as `Authorization: Bearer`; `ARTIFACTORY_URL` sets the base.\n\n**Parameters:**\n- `buildName` (required): the build name as published, e.g. `my-app-release`. Spaces are allowed and encoded for you.\n- `buildNumber` (required): the build number string, e.g. `142`.\n- `project`: JFrog project key when the build was published into a project; omit for the global scope.\n\nRead-only. Build promotion and deletion are not available.\n\n**IMPORTANT - Cost Optimization:** build info can be large — use `jq`, e.g. `buildInfo.modules[*].{id: id, artifacts: artifacts[*].name}` or `buildInfo.vcs[*].revision`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://jfrog.com/help/r/jfrog-rest-apis/build-info\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "buildName",
        "buildNumber"
      ],
      "type": "object",
      "properties": {
        "buildName": {
          "description": "Build name as published to Artifactory, e.g. `my-app-release`. Spaces\nare allowed.",
          "type": "string"
        },
        "buildNumber": {
          "description": "Build number (or build id string), e.g. `142` or `2024.06.01-3`.",
          "type": "string"
        },
        "project": {
          "description": "JFrog project key the build belongs to. Omit for the default (global)\nproject.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"buildInfo.modules[*].{id: id, artifacts: artifacts[*].name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "artifactory_get_file_info",
    "description": "Get JFrog Artifactory **item metadata** for one file or folder — size, checksums (`md5`, `sha1`, `sha256`), created/modified timestamps and who last modified it, or the child listing of a folder. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/storage/{repo}/{path}`. For a **file** the response carries `repo`, `path`, `size`, `created`, `createdBy`, `lastModified`, `modifiedBy`, `mimeType`, `checksums`, `originalChecksums`, `downloadUri`, and `uri`. For a **folder** it carries `children[]` (each `{uri: \"/name\", folder: true|false}`) instead of size/checksums — call again with the child appended to `path` to descend.\n\nAuthenticates with a JFrog **access token** (`ARTIFACTORY_TOKEN`) sent as `Authorization: Bearer`; `ARTIFACTORY_URL` sets the base.\n\n**Parameters:**\n- `repo` (required): repository key, e.g. `libs-release-local`.\n- `path` (required): file or folder path inside the repo, e.g. `com/example/my-app/1.4.2/my-app-1.4.2.jar`. A leading slash is optional; `..` segments are rejected.\n\nRead-only: this returns metadata only and never downloads the file's bytes. `downloadUri` is informational.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{size: size, sha256: checksums.sha256, modified: lastModified}` or `children[*].uri`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://jfrog.com/help/r/jfrog-rest-apis/file-info\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "repo",
        "path"
      ],
      "type": "object",
      "properties": {
        "repo": {
          "description": "Repository key, e.g. `libs-release-local`.",
          "type": "string"
        },
        "path": {
          "description": "Path of the file or folder inside the repository, e.g.\n`com/example/my-app/1.4.2/my-app-1.4.2.jar` or `com/example/my-app`.\nA leading slash is optional.",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"{size: size, sha256: checksums.sha256, modified: lastModified}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "artifactory_list_repositories",
    "description": "List the JFrog Artifactory **repositories** the token can see — the first step to find where a package or build output lives. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/repositories`. Each entry carries `key`, `type` (`LOCAL`, `REMOTE`, `VIRTUAL`, `FEDERATED`, `DISTRIBUTION`), `packageType` (`Maven`, `Npm`, `Docker`, `Generic`, …), `description`, and `url`. Use the `key` as `repo` in `artifactory_search` and `artifactory_get_file_info`.\n\nAuthenticates with a JFrog **access token** (`ARTIFACTORY_TOKEN`) sent as `Authorization: Bearer`; `ARTIFACTORY_URL` sets the base (an origin such as `https://mycorp.jfrog.io` gains the `/artifactory` context automatically). No per-call auth is needed.\n\n**Parameters (all optional):**\n- `type`: repository class — `local`, `remote`, `virtual`, `federated`, or `distribution`.\n- `packageType`: e.g. `maven`, `npm`, `docker`, `pypi`, `generic`, `nuget`, `helm`.\n\nRead-only. The full list is returned in one call (Artifactory does not page this endpoint); on large instances filter by `type`/`packageType` and use `jq`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{key: key, type: type, packageType: packageType}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://jfrog.com/help/r/jfrog-rest-apis/get-repositories\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "type": {
          "description": "Repository class to list: `local`, `remote`, `virtual`, `federated`,\nor `distribution`. Omit for every repository the token can see.",
          "type": [
            "string",
            "null"
          ]
        },
        "packageType": {
          "description": "Package type filter, e.g. `maven`, `npm`, `docker`, `pypi`, `generic`,\n`nuget`, `helm`. Omit for all package types.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{key: key, type: type, packageType: packageType}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "artifactory_search",
    "description": "Search JFrog Artifactory **items** (files or folders) with structured filters — find a specific artifact, every version of a package, what changed since a date, or where a binary with a known checksum is stored. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nThe filters are compiled server-side into a single bounded AQL query (`items.find({...}).include(...).sort(...).limit(N)`) and sent to `POST /api/search/aql`. You never write AQL yourself, and every value is escaped — a `name` of `\"x\"` is searched literally.\n\n**Result fields** (in `results[]`): `repo`, `path` (folder inside the repo), `name` (file name), `type`, `size` (bytes), `modified`, `created`, `sha256`, `actual_sha1`. `range` reports `total` matched versus `end_pos` returned. Combine `repo/path/name` to address an item in `artifactory_get_file_info`.\n\n**Parameters** — at least one of `repo`, `name`, `path`, `sha256` is required:\n- `repo`: one repository key (exact), e.g. `libs-release-local`.\n- `name`: file name. With `*` it is a wildcard pattern (`my-app-*.jar`), otherwise exact.\n- `path`: folder path inside the repo, no leading slash. With `*` it is a wildcard pattern (`com/example/*`), otherwise exact.\n- `type`: `file` (default), `folder`, or `any`.\n- `modifiedAfter`: ISO-8601 timestamp, e.g. `2024-06-01T00:00:00.000Z` — only items modified after it.\n- `sha256`: 64-hex checksum; finds every copy of that exact binary.\n- `limit`: 1–500, default 100. **Results are cut at `limit`** — nothing beyond it is returned, so narrow the filters or sort (`sortBy: \"modified\", sortOrder: \"desc\"`) to reach the items you want.\n- `sortBy`: `name`, `modified`, `size`, or `path`; `sortOrder`: `asc` (default) or `desc`.\n\nRead-only. Downloads are not performed by this tool.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `results[*].{repo: repo, path: path, name: name, size: size, modified: modified}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAQL reference: https://jfrog.com/help/r/jfrog-artifactory-documentation/artifactory-query-language\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "repo": {
          "description": "Restrict to one repository by key, e.g. `libs-release-local`. Exact\nmatch.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "description": "Item name (file name) to find, e.g. `my-app-1.4.2.jar`. A value\ncontaining `*` is a wildcard pattern (`*.jar`, `my-app-*`); without\n`*` it is an exact match.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Folder path inside the repository (no leading slash), e.g.\n`com/example/my-app/1.4.2`. A value containing `*` is a wildcard\npattern (`com/example/*`); without `*` it is an exact match.",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "description": "Item kind: `file` (default), `folder`, or `any`.",
          "type": [
            "string",
            "null"
          ]
        },
        "modifiedAfter": {
          "description": "Only items modified after this ISO-8601 timestamp, e.g.\n`2024-06-01T00:00:00.000Z`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sha256": {
          "description": "Find the item(s) with this exact SHA-256 checksum (64 hex characters).\nUseful to locate where a known binary is stored.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Maximum number of results, 1–500. Default 100. Results beyond the\nlimit are not returned; narrow the filters or sort to find them.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "sortBy": {
          "description": "Sort field: `name`, `modified`, `size`, or `path`. Omit for\nArtifactory's natural order.",
          "type": [
            "string",
            "null"
          ]
        },
        "sortOrder": {
          "description": "Sort direction: `asc` (default when `sortBy` is set) or `desc`. Use\n`sortBy: \"modified\", sortOrder: \"desc\"` for the newest items first.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"results[*].{repo: repo, path: path, name: name, size: size}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_clone",
    "description": "Clone a Bitbucket repository to your local filesystem using SSH (preferred) or HTTPS.\n\nProvide `repoSlug` and `targetPath` (absolute path). Clones into `targetPath/repoSlug`. SSH keys must be configured; falls back to HTTPS if unavailable.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "repoSlug",
        "targetPath"
      ],
      "properties": {
        "workspaceSlug": {
          "description": "Bitbucket workspace slug containing the repository. If not provided,\nthe tool will use your default workspace (either configured via\n`BITBUCKET_DEFAULT_WORKSPACE` or the first workspace in your account).\nExample: \"myteam\"",
          "type": [
            "string",
            "null"
          ]
        },
        "repoSlug": {
          "description": "Repository name/slug to clone. This is the short name of the\nrepository. Example: \"project-api\"",
          "type": "string"
        },
        "targetPath": {
          "description": "Directory path where the repository will be cloned. IMPORTANT:\nAbsolute paths are strongly recommended (e.g., \"/home/user/projects\"\nor \"C:\\\\Users\\\\name\\\\projects\"). Relative paths will be resolved\nrelative to the server's working directory, which may not be what you\nexpect. The repository will be cloned into a subdirectory at\ntargetPath/repoSlug. Make sure you have write permissions to this\nlocation.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_delete",
    "description": "Delete Bitbucket resources. Returns TOON format by default.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Delete branch:** `/repositories/{workspace}/{repo}/refs/branches/{branch_name}`\n2. **Delete PR comment:** `/repositories/{workspace}/{repo}/pullrequests/{pr_id}/comments/{comment_id}`\n3. **Decline PR:** `/repositories/{workspace}/{repo}/pullrequests/{id}/decline`\n4. **Remove PR approval:** `/repositories/{workspace}/{repo}/pullrequests/{id}/approve`\n5. **Delete repository:** `/repositories/{workspace}/{repo}` (caution: irreversible)\n\nNote: Most DELETE endpoints return 204 No Content on success.\n\nThe `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_get",
    "description": "Read any Bitbucket data. Returns TOON format by default (30-60% fewer tokens than JSON).\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` param to filter response fields. Unfiltered responses are very expensive!\n- Use `pagelen` query param to restrict result count (e.g., `pagelen: \"5\"`)\n- If unsure about available fields, first fetch ONE item with `pagelen: \"1\"` and NO jq filter to explore the schema, then use jq in subsequent calls\n\n**Schema Discovery Pattern:**\n1. First call: `path: \"/workspaces\", queryParams: {\"pagelen\": \"1\"}` (no jq) - explore available fields\n2. Then use: `jq: \"values[*].{slug: slug, name: name, uuid: uuid}\"` - extract only what you need\n\n**Output format:** TOON (default, token-efficient) or JSON (`outputFormat: \"json\"`)\n\n**Common paths:**\n- `/workspaces` - list workspaces\n- `/repositories/{workspace}` - list repos in workspace\n- `/repositories/{workspace}/{repo}` - get repo details\n- `/repositories/{workspace}/{repo}/pullrequests` - list PRs\n- `/repositories/{workspace}/{repo}/pullrequests/{id}` - get PR details\n- `/repositories/{workspace}/{repo}/pullrequests/{id}/comments` - list PR comments\n- `/repositories/{workspace}/{repo}/pullrequests/{id}/diff` - get PR diff\n- `/repositories/{workspace}/{repo}/refs/branches` - list branches\n- `/repositories/{workspace}/{repo}/commits` - list commits\n- `/repositories/{workspace}/{repo}/src/{commit}/{filepath}` - get file content\n- `/repositories/{workspace}/{repo}/diff/{source}..{destination}` - compare branches/commits\n\n**Query params:** `pagelen` (page size), `page` (page number), `q` (filter), `sort` (order), `fields` (sparse response)\n\n**Example filters (q param):** `state=\"OPEN\"`, `source.branch.name=\"feature\"`, `title~\"bug\"`\n\n**JQ examples:** `values[*].slug`, `values[0]`, `values[*].{name: name, uuid: uuid}`\n\nThe `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_patch",
    "description": "Partially update Bitbucket resources. Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:** Use `jq` param to filter response fields.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update PR title/description:** `/repositories/{workspace}/{repo}/pullrequests/{id}`\n   body: `{\"title\": \"New title\", \"description\": \"Updated description\"}`\n\n2. **Update PR reviewers:** `/repositories/{workspace}/{repo}/pullrequests/{id}`\n   body: `{\"reviewers\": [{\"uuid\": \"{user-uuid}\"}]}`\n\n3. **Update repository properties:** `/repositories/{workspace}/{repo}`\n   body: `{\"description\": \"New description\"}`\n\n4. **Update comment:** `/repositories/{workspace}/{repo}/pullrequests/{pr_id}/comments/{comment_id}`\n   body: `{\"content\": {\"raw\": \"Updated comment\"}}`\n\nThe `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_post",
    "description": "Create Bitbucket resources. Returns TOON format by default (token-efficient).\n\n**IMPORTANT - Cost Optimization:**\n- Use `jq` param to extract only needed fields from response (e.g., `jq: \"{id: id, title: title}\"`)\n- Unfiltered responses include all metadata and are expensive!\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Create PR:** `/repositories/{workspace}/{repo}/pullrequests`\n   body: `{\"title\": \"...\", \"source\": {\"branch\": {\"name\": \"feature\"}}, \"destination\": {\"branch\": {\"name\": \"main\"}}}`\n\n2. **Add PR comment:** `/repositories/{workspace}/{repo}/pullrequests/{id}/comments`\n   body: `{\"content\": {\"raw\": \"Comment text\"}}`\n\n3. **Approve PR:** `/repositories/{workspace}/{repo}/pullrequests/{id}/approve`\n   body: `{}`\n\n4. **Request changes:** `/repositories/{workspace}/{repo}/pullrequests/{id}/request-changes`\n   body: `{}`\n\n5. **Merge PR:** `/repositories/{workspace}/{repo}/pullrequests/{id}/merge`\n   body: `{\"merge_strategy\": \"squash\"}` (strategies: merge_commit, squash, fast_forward)\n\nThe `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_put",
    "description": "Replace Bitbucket resources (full update). Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:**\n- Use `jq` param to extract only needed fields from response\n- Example: `jq: \"{uuid: uuid, name: name}\"`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update repository:** `/repositories/{workspace}/{repo}`\n   body: `{\"description\": \"...\", \"is_private\": true, \"has_issues\": true}`\n\n2. **Create/update file:** `/repositories/{workspace}/{repo}/src`\n   Note: Use multipart form data for file uploads (complex - prefer PATCH for metadata)\n\n3. **Update branch restriction:** `/repositories/{workspace}/{repo}/branch-restrictions/{id}`\n   body: `{\"kind\": \"push\", \"pattern\": \"main\", \"users\": [{\"uuid\": \"...\"}]}`\n\nThe `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "bb_upload",
    "description": "Upload files to a Bitbucket repository's **Downloads** section (`POST /repositories/{workspace}/{repo}/downloads`, multipart). Use this instead of `bb_post` for anything that is a file: Postman collections, HTTP test files, reports, archives. Returns TOON by default (`outputFormat: \"json\"` for JSON).\n\n**Sending content:** the server cannot read paths on your machine. Send each file as `filename` + `contentBase64` (standard base64 of the bytes) with an optional `mimeType`, or set `artifactId` to upload a temporary artifact another tool returned (for example `circleci_logs`) without moving its bytes through the client.\n\n**Collisions:** Bitbucket silently replaces an artifact that has the same name. This tool lists the repository's artifacts first and refuses the whole upload (HTTP 409, nothing sent) when any name already exists, unless you pass `onConflict: \"replace\"`. The check and the upload are two requests, so an artifact created by someone else in between is still replaced.\n\n**Limits:** up to 20 files per call; per-file and per-call decoded size limits are set by the server (`UPLOAD_MAX_FILE_BYTES`, default 10 MiB; `UPLOAD_MAX_TOTAL_BYTES`, default 25 MiB). Over streamable HTTP the whole tool call must also fit the server's request body cap (`HTTP_REQUEST_BODY_LIMIT_BYTES`, 1 MB by default), so prefer `artifactId` for large files there unless the operator raised it. Filenames are one path segment: no `/`, `\\`, `:`, `*`, `?`, `\"`, `<`, `>`, `|`, control characters, or `..`.\n\n**Failures:** permission (403), rate limit (429), and validation errors are returned without retrying and state that nothing was uploaded. A timeout or 5xx after the body was sent is reported as an *unknown outcome*: list the Downloads with `bb_get` before retrying, because a retry replaces whatever did land.\n\n**Example — publish a Postman collection, then link it from a PR comment:**\n\n1. `bb_upload` with `repoSlug: \"project-api\"`, `files: [{\"filename\": \"project-api.postman_collection.json\", \"mimeType\": \"application/json\", \"contentBase64\": \"<base64 of the collection>\"}]`\n   → result includes `files[0].browserUrl`, e.g. `https://bitbucket.org/myteam/project-api/downloads/project-api.postman_collection.json`\n2. `bb_post` to `/repositories/myteam/project-api/pullrequests/42/comments` with body `{\"content\": {\"raw\": \"Postman collection for this change: https://bitbucket.org/myteam/project-api/downloads/project-api.postman_collection.json\"}}`\n\nEach result entry has `name`, `size`, `mimeType`, `downloadUrl` (API link, redirects to the bytes), `browserUrl`, and `replaced`. The `/2.0` prefix is added automatically. API reference: https://developer.atlassian.com/cloud/bitbucket/rest/api-group-downloads/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "UploadFileArg": {
          "description": "One file in a `bb_upload` call.",
          "type": "object",
          "properties": {
            "filename": {
              "description": "Artifact name as it will appear under Downloads and in its URL. One\npath segment of at most 255 bytes: no `/`, `\\`, `:`, `*`, `?`, `\"`,\n`<`, `>`, `|`, control characters, leading/trailing whitespace, or\n`..`. Example: \"postman-collection.json\"",
              "type": "string"
            },
            "mimeType": {
              "description": "MIME type sent with the file, e.g. \"application/json\". Defaults to\n\"application/octet-stream\", or to the source artifact's own type when\n`artifactId` is used.",
              "type": [
                "string",
                "null"
              ]
            },
            "contentBase64": {
              "description": "File contents as standard base64 (padding optional; not a `data:` URL).\nRequired unless `artifactId` is given. Use this from remote MCP\nclients: the server cannot read paths on the client machine.",
              "type": [
                "string",
                "null"
              ]
            },
            "artifactId": {
              "description": "Upload a server-side temporary artifact returned by another tool (for\nexample `circleci_logs`) without round-tripping its bytes through the\nclient. Mutually exclusive with `contentBase64`.",
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "filename"
          ]
        },
        "ConflictPolicy": {
          "description": "Where an uploaded artifact's name collides with an existing one.",
          "oneOf": [
            {
              "description": "Refuse the whole upload before sending anything (default).",
              "type": "string",
              "const": "fail"
            },
            {
              "description": "Overwrite the existing artifact deliberately.",
              "type": "string",
              "const": "replace"
            }
          ]
        },
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "repoSlug",
        "files"
      ],
      "type": "object",
      "properties": {
        "workspaceSlug": {
          "description": "Bitbucket workspace slug containing the repository. If not provided,\nthe tool will use your default workspace (either configured via\n`BITBUCKET_DEFAULT_WORKSPACE` or the first workspace in your account).\nExample: \"myteam\"",
          "type": [
            "string",
            "null"
          ]
        },
        "repoSlug": {
          "description": "Repository slug that owns the Downloads section. Example: \"project-api\"",
          "type": "string"
        },
        "files": {
          "description": "Files to upload in one request (1 to 20). Every entry needs `filename`\nplus exactly one of `contentBase64` or `artifactId`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/UploadFileArg"
          }
        },
        "onConflict": {
          "description": "What to do when an artifact with the same name already exists:\n\"fail\" (default) refuses the whole upload before sending anything,\nbecause Bitbucket replaces same-name artifacts silently; \"replace\"\noverwrites them deliberately.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConflictPolicy"
            },
            {
              "type": "null"
            }
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_delete",
    "description": "Delete CircleCI resources via the CircleCI REST API v2 (HTTP DELETE). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a personal API token (`CIRCLECI_TOKEN`) sent as a Bearer token; no per-call auth is needed.\n\n**Common DELETE endpoints:**\n- `/project/{project-slug}/envvar/{name}` - delete a project environment variable.\n- `/project/{project-slug}/schedule/{schedule-id}` - delete a scheduled pipeline.\n- `/context/{context-id}` - delete a context.\n\n**Project slug:** `<vcs>/<org>/<repo>` — e.g. `gh/acme/web`, `bb/acme/web`, or `circleci/<org-id>/<project-id>`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://circleci.com/docs/api/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_get",
    "description": "Read any CircleCI data via the CircleCI REST API v2. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a personal API token (`CIRCLECI_TOKEN`) sent as a Bearer token; no per-call auth is needed.\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` to filter response fields. Unfiltered responses are expensive.\n- To explore a schema, fetch ONE item (or a single list page) with NO jq, then add a jq filter on later calls.\n\n**Project slug:** most project-scoped paths take a `project-slug` of the form `<vcs>/<org>/<repo>` — e.g. `gh/acme/web` (GitHub), `bb/acme/web` (Bitbucket), or `circleci/<org-id>/<project-id>`.\n\n**Listing / \"search\":** CircleCI has no single search tool — listing is just GETs against the right path:\n- `/me` - the authenticated user\n- `/project/{project-slug}` - project details\n- `/project/{project-slug}/pipeline` - pipelines for a project (`branch` query param to filter)\n- `/project/{project-slug}/pipeline/mine` - pipelines you triggered\n- `/pipeline/{pipeline-id}` - a single pipeline\n- `/pipeline/{pipeline-id}/workflow` - workflows in a pipeline\n- `/workflow/{workflow-id}` - a single workflow's status\n- `/workflow/{workflow-id}/job` - jobs in a workflow\n- `/project/{project-slug}/job/{job-number}` - a single job\n- `/insights/{project-slug}/workflows` - workflow insights/metrics\n- `/project/{project-slug}/envvar` - project environment variables (names only)\n\n**Linking from a PR / branch (cross-vendor):** CircleCI is keyed by branch, not by pull request. To find the build for a Bitbucket/GitHub PR, take the PR's source branch and call `/project/{project-slug}/pipeline?branch={branch}`, then drill down: latest pipeline `id` → `/pipeline/{id}/workflow` → `/workflow/{workflow-id}/job` (each job has `status` + `job_number`). For *why* a job failed, prefer `/project/{project-slug}/{job-number}/tests` (failed test names + messages, when test results are stored) over raw logs — raw step logs live on CircleCI's v1.1 API / S3 output URLs, outside this v2 base.\n\n**Pagination:** CircleCI uses a `next_page_token` in the response. Pass it back as the `page-token` query param to fetch the next page.\n\n**Deployment monitoring and freshness:** Pipeline, workflow, and job paths (including discovery lists) always fetch upstream and are never stored in the MCP HTTP cache, even if CircleCI sends a long `max-age`. Poll `/pipeline/{id}/workflow`, then `/workflow/{id}` or `/workflow/{id}/job` until the target workflow/jobs reach a terminal status; queued, running, and approval-waiting states are not completion. A pipeline being created does not mean its deployment succeeded. Terminal status reads also remain uncached to observe reruns.\n\nFor any other GET, set top-level `fresh: true` to bypass MCP cache lookup and storage and discard the previous entry. For example: `{\"path\":\"/workflow/df722a5b-dd0f-437c-a426-efbda3a7a597/job\",\"fresh\":true,\"jq\":\"items[*].{name:name,status:status}\",\"outputFormat\":\"json\"}`. This is a local control, never an upstream query parameter. Do not add `_fresh` timestamps to `queryParams` or the path.\n\n**Response metadata:** GET results have a `data` and `cache` envelope in both formats. `jq` filters the upstream body before it is placed in `data`; metadata is always retained. `cache.hit` reports local MCP cache reuse, `cache.ageMs` is elapsed local cache residence (zero on an upstream fetch), and `cache.fetchedAt` is the UTC timestamp when MCP finished reading that upstream body. Hits retain the original fetch timestamp and do not extend TTL. These fields describe MCP provenance, not when CircleCI last updated its state or whether an upstream proxy cached it. JSON example: `{\"data\":[{\"status\":\"success\"}],\"cache\":{\"hit\":false,\"ageMs\":0,\"fetchedAt\":\"2026-09-09T16:39:07Z\"}}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `items[*].{id: id, state: state, created: created_at}`, `items[*].number`, `{login: login, id: id}`\n\nAPI reference: https://circleci.com/docs/api/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        },
        "fresh": {
          "description": "Fetch from upstream, bypassing MCP cache lookup and storage. Never sent as a query parameter.",
          "type": "boolean",
          "default": false
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_logs",
    "description": "Fetch raw CircleCI step logs for a job. Use this after `circleci_get /workflow/{workflow-id}/job` gives you a job's `job_number`.\n\nInputs:\n- `projectSlug`: CircleCI project slug in v2 form, e.g. `gh/acme/web` or `bb/acme/web`.\n- `jobNumber`: Numeric `job_number` from the workflow job list.\n- `stepNumber` (optional): fetch only one one-based step number.\n- `failedOnly` (optional): fetch only failed/non-zero-exit actions, avoiding downloads for successful actions.\n- `condensed` (optional): return error-like lines with surrounding context.\n- `contextLines` (optional): surrounding lines for condensed output (default 3, maximum 20).\n\nThis tool uses CircleCI's older build-details API to discover per-step output URLs, then fetches and flattens those step outputs into readable log text. `circleci/<org-id>/<project-id>` slugs are not supported because CircleCI's older log endpoint is VCS-path based.\n\nThe complete selected logs are atomically saved to a process-owned temporary file. The response includes its local path, opaque artifact ID, and HTTP download path. HTTP clients can resume with standard Range requests; other MCP clients can call `artifact_read` repeatedly with `nextOffset`. The inline response contains a diagnostic summary plus either a small head/large tail preview or condensed error context. Temporary artifacts are deleted on graceful process exit; abandoned artifacts are removed the next time the server starts.\n\nSelected action outputs are fetched with bounded concurrency while preserving CircleCI order. Transient connection/timeouts and HTTP 429/502/503/504 responses are retried up to three attempts with backoff and `Retry-After` support.\n\nAction bodies stream directly into atomic part artifacts with chunk-level byte accounting. The ordered final JSON artifact is assembled from those files with a fixed-size copy buffer, avoiding retention of complete action bodies and a second full serialized copy in memory.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "projectSlug",
        "jobNumber"
      ],
      "type": "object",
      "properties": {
        "projectSlug": {
          "description": "CircleCI project slug in v2 form: \"gh/org/repo\" or \"bb/org/repo\".",
          "type": "string"
        },
        "jobNumber": {
          "description": "CircleCI job number from `/workflow/{workflow-id}/job`.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "stepNumber": {
          "description": "Retrieve only this one-based CircleCI step number. Omit to consider all\nsteps in the job.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "failedOnly": {
          "description": "Retrieve only actions whose status is failed or whose exit code is\nnon-zero. This avoids downloading successful action output.",
          "type": "boolean",
          "default": false
        },
        "condensed": {
          "description": "Return error-relevant lines with surrounding context instead of the\nnormal head/tail preview. The complete selected logs are still saved.",
          "type": "boolean",
          "default": false
        },
        "contextLines": {
          "description": "Number of lines before and after each condensed match (default 3,\nmaximum 20).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_patch",
    "description": "Partially update CircleCI resources via the CircleCI REST API v2 (HTTP PATCH). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a personal API token (`CIRCLECI_TOKEN`) sent as a Bearer token; no per-call auth is needed.\n\n**Common PATCH endpoints:**\n- `/project/{project-slug}/schedule/{schedule-id}` - update a scheduled pipeline (name, timetable, parameters).\n\n**Project slug:** `<vcs>/<org>/<repo>` — e.g. `gh/acme/web`, `bb/acme/web`, or `circleci/<org-id>/<project-id>`.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://circleci.com/docs/api/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_post",
    "description": "Create CircleCI resources via the CircleCI REST API v2 (HTTP POST). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a personal API token (`CIRCLECI_TOKEN`) sent as a Bearer token; no per-call auth is needed.\n\n**Common POST endpoints:**\n- `/project/{project-slug}/pipeline` - trigger a pipeline. Body: `{\"branch\": \"main\"}` or `{\"tag\": \"v1.0\"}`, optionally `{\"parameters\": {...}}`.\n- `/workflow/{workflow-id}/cancel` - cancel a running workflow (no body).\n- `/workflow/{workflow-id}/rerun` - rerun a workflow. Body may include `{\"from_failed\": true}` or `{\"jobs\": [...]}`.\n- `/project/{project-slug}/job/{job-number}/cancel` - cancel a job (no body).\n- `/project/{project-slug}/envvar` - create an env var. Body: `{\"name\": \"FOO\", \"value\": \"bar\"}`.\n\n**Project slug:** `<vcs>/<org>/<repo>` — e.g. `gh/acme/web`, `bb/acme/web`, or `circleci/<org-id>/<project-id>`.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://circleci.com/docs/api/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "circleci_put",
    "description": "Update/replace CircleCI resources via the CircleCI REST API v2 (HTTP PUT). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a personal API token (`CIRCLECI_TOKEN`) sent as a Bearer token; no per-call auth is needed.\n\nPUT is rarely used in the CircleCI v2 API — most mutations are POST (create/trigger) or DELETE. Use it only for endpoints documented to accept PUT.\n\n**Project slug:** `<vcs>/<org>/<repo>` — e.g. `gh/acme/web`, `bb/acme/web`, or `circleci/<org-id>/<project-id>`.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://circleci.com/docs/api/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "conf_delete",
    "description": "Delete Confluence resources. Returns TOON format by default.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n- `/wiki/api/v2/pages/{id}` - Delete page\n- `/wiki/api/v2/blogposts/{id}` - Delete blog post\n- `/wiki/api/v2/pages/{id}/labels/{label-id}` - Remove label\n- `/wiki/api/v2/footer-comments/{id}` - Delete comment\n- `/wiki/api/v2/attachments/{id}` - Delete attachment\n\nNote: Most DELETE endpoints return 204 No Content on success.\n\nAPI reference: https://developer.atlassian.com/cloud/confluence/rest/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "conf_get",
    "description": "Read any Confluence data. Returns TOON format by default (30-60% fewer tokens than JSON).\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` param to filter response fields. Unfiltered responses are very expensive!\n- Use `limit` query param to restrict result count (e.g., `limit: \"5\"`)\n- If unsure about available fields, first fetch ONE item with `limit: \"1\"` and NO jq filter to explore the schema, then use jq in subsequent calls\n\n**Schema Discovery Pattern:**\n1. First call: `path: \"/wiki/api/v2/spaces\", queryParams: {\"limit\": \"1\"}` (no jq) - explore available fields\n2. Then use: `jq: \"results[*].{id: id, key: key, name: name}\"` - extract only what you need\n\n**Output format:** TOON (default, token-efficient) or JSON (`outputFormat: \"json\"`)\n\n**Common paths:**\n- `/wiki/api/v2/spaces` - list spaces\n- `/wiki/api/v2/pages` - list pages (use `space-id` query param)\n- `/wiki/api/v2/pages/{id}` - get page details\n- `/wiki/api/v2/pages/{id}/body` - get page body (`body-format`: storage, atlas_doc_format, view)\n- `/wiki/rest/api/search` - search content (`cql` query param)\n\n**JQ examples:** `results[*].id`, `results[0]`, `results[*].{id: id, title: title}`\n\nAPI reference: https://developer.atlassian.com/cloud/confluence/rest/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "conf_patch",
    "description": "Partially update Confluence resources. Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:** Use `jq` param to filter response fields.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update space:** `/wiki/api/v2/spaces/{id}`\n   body: `{\"name\": \"New Name\", \"description\": {\"plain\": {\"value\": \"Desc\", \"representation\": \"plain\"}}}`\n\n2. **Update comment:** `/wiki/api/v2/footer-comments/{id}`\n\nNote: Confluence v2 API primarily uses PUT for updates.\n\nAPI reference: https://developer.atlassian.com/cloud/confluence/rest/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "conf_post",
    "description": "Create Confluence resources. Returns TOON format by default (token-efficient).\n\n**IMPORTANT - Cost Optimization:**\n- Use `jq` param to extract only needed fields from response (e.g., `jq: \"{id: id, title: title}\"`)\n- Unfiltered responses include all metadata and are expensive!\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Create page:** `/wiki/api/v2/pages`\n   body: `{\"spaceId\": \"123456\", \"status\": \"current\", \"title\": \"Page Title\", \"parentId\": \"789\", \"body\": {\"representation\": \"storage\", \"value\": \"<p>Content</p>\"}}`\n\n2. **Create blog post:** `/wiki/api/v2/blogposts`\n   body: `{\"spaceId\": \"123456\", \"status\": \"current\", \"title\": \"Blog Title\", \"body\": {\"representation\": \"storage\", \"value\": \"<p>Content</p>\"}}`\n\n3. **Add label:** `/wiki/api/v2/pages/{id}/labels` - body: `{\"name\": \"label-name\"}`\n\n4. **Add comment:** `/wiki/api/v2/pages/{id}/footer-comments`\n\nAPI reference: https://developer.atlassian.com/cloud/confluence/rest/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "conf_put",
    "description": "Replace Confluence resources (full update). Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:**\n- Use `jq` param to extract only needed fields from response\n- Example: `jq: \"{id: id, version: version.number}\"`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update page:** `/wiki/api/v2/pages/{id}`\n   body: `{\"id\": \"123\", \"status\": \"current\", \"title\": \"Updated Title\", \"spaceId\": \"456\", \"body\": {\"representation\": \"storage\", \"value\": \"<p>Content</p>\"}, \"version\": {\"number\": 2}}`\n   Note: version.number must be incremented\n\n2. **Update blog post:** `/wiki/api/v2/blogposts/{id}`\n\nNote: PUT replaces entire resource. Version number must be incremented.\n\nAPI reference: https://developer.atlassian.com/cloud/confluence/rest/v2/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_comment_create",
    "description": "Create a response/comment in an edX/Open edX discussion thread.\n\nUses `POST /api/discussion/v1/comments/`.\n\nProvide `threadId` to respond to a thread, or `parentId` for a nested comment\nwhen the target Open edX instance supports that shape. `rawBody` is required.\nRequires `EDX_ACCESS_TOKEN` and discussion posting access.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "rawBody"
      ],
      "type": "object",
      "properties": {
        "threadId": {
          "description": "Thread ID to respond to. Omit only when creating a child comment under\n`parentId`, if the target Open edX instance supports that form.",
          "type": [
            "string",
            "null"
          ]
        },
        "parentId": {
          "description": "Parent comment ID for a nested comment.",
          "type": [
            "string",
            "null"
          ]
        },
        "rawBody": {
          "description": "Raw body. May contain Markdown, HTML, and MathJax markup supported by\nthe edX discussion service.",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_comments",
    "description": "List comments/responses for an edX/Open edX discussion thread.\n\nUses `GET /api/discussion/v1/comments/` with `thread_id`.\n\nSupports `endorsed`, `pageSize`, and `page`. Requires `EDX_ACCESS_TOKEN`; the\nuser behind the token must have access to the thread's course discussion.\n\nUse `jq` to extract compact fields such as authors, bodies, vote counts, and\nchild comments.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "threadId"
      ],
      "type": "object",
      "properties": {
        "threadId": {
          "description": "Thread ID whose comments/responses should be listed.",
          "type": "string"
        },
        "endorsed": {
          "description": "Retrieve only endorsed or non-endorsed comments.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pageSize": {
          "description": "Number of comments per page.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "Page number to retrieve.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_course",
    "description": "Get edX/Open edX discussion metadata for a course.\n\nUses `GET /api/discussion/v1/courses/{course_id}/`.\n\nRequires `EDX_ACCESS_TOKEN`. The user behind the token must have access to the\ncourse. Set `EDX_API_BASE` for non-edx.org Open edX LMS hosts.\n\nUse `jq` to keep responses small, for example:\n- `{enabled: discussions_enabled, topics: topics_url}`\n- `{threads: thread_list_url, following: following_thread_list_url}`\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "courseId"
      ],
      "type": "object",
      "properties": {
        "courseId": {
          "description": "Course key, e.g. \"course-v1:edX+DemoX+Demo_Course\".",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_thread_create",
    "description": "Create an edX/Open edX discussion thread.\n\nUses `POST /api/discussion/v1/threads/`.\n\nRequired fields are `courseId`, `topicId`, `type` (`discussion` or `question`),\n`title`, and `rawBody`. Some courses may block posting during blackouts or after\ncourse/discussion closure. Requires `EDX_ACCESS_TOKEN` and course discussion\naccess.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "EdxDiscussionThreadType": {
          "type": "string",
          "enum": [
            "discussion",
            "question"
          ]
        },
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "courseId",
        "topicId",
        "type",
        "title",
        "rawBody"
      ],
      "type": "object",
      "properties": {
        "courseId": {
          "description": "Course key.",
          "type": "string"
        },
        "topicId": {
          "description": "Topic ID to create the thread in.",
          "type": "string"
        },
        "type": {
          "description": "Thread type: \"discussion\" or \"question\".",
          "$ref": "#/$defs/EdxDiscussionThreadType"
        },
        "title": {
          "description": "Thread title.",
          "type": "string"
        },
        "rawBody": {
          "description": "Raw body. May contain Markdown, HTML, and MathJax markup supported by\nthe edX discussion service.",
          "type": "string"
        },
        "groupId": {
          "description": "Optional cohort/group ID. Privileged roles may set this explicitly.",
          "type": [
            "integer",
            "null"
          ],
          "format": "int64"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_threads",
    "description": "List or search edX/Open edX discussion threads.\n\nUses `GET /api/discussion/v1/threads/` with `course_id` and optional filters.\n\nCommon filters:\n- `topicId`: threads in a specific topic\n- `textSearch`: search matching thread/comment text\n- `following`: only followed threads\n- `view`: `unread` or `unanswered`\n- `orderBy`: `last_activity_at`, `comment_count`, or `vote_count`\n\n`topicId`, `following`, and `textSearch` are mutually exclusive in the edX\nDiscussion API. Requires `EDX_ACCESS_TOKEN` and course discussion access.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "EdxDiscussionThreadView": {
          "type": "string",
          "enum": [
            "unread",
            "unanswered"
          ]
        },
        "EdxDiscussionThreadOrderBy": {
          "type": "string",
          "enum": [
            "last_activity_at",
            "comment_count",
            "vote_count"
          ]
        },
        "EdxDiscussionOrderDirection": {
          "type": "string",
          "enum": [
            "asc",
            "desc"
          ]
        },
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "courseId"
      ],
      "type": "object",
      "properties": {
        "courseId": {
          "description": "Course key. Required by the edX Discussion API.",
          "type": "string"
        },
        "topicId": {
          "description": "Retrieve threads only within this topic. Mutually exclusive with\n`following` and `textSearch`.",
          "type": [
            "string",
            "null"
          ]
        },
        "following": {
          "description": "Retrieve only threads the user is following.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "view": {
          "description": "Retrieve only unread threads or unanswered question threads.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdxDiscussionThreadView"
            },
            {
              "type": "null"
            }
          ]
        },
        "textSearch": {
          "description": "Full-text search query for matching threads/comments. Mutually\nexclusive with `topicId` and `following`.",
          "type": [
            "string",
            "null"
          ]
        },
        "orderBy": {
          "description": "Order threads by last activity, comment count, or vote count.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdxDiscussionThreadOrderBy"
            },
            {
              "type": "null"
            }
          ]
        },
        "orderDirection": {
          "description": "Sort direction.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdxDiscussionOrderDirection"
            },
            {
              "type": "null"
            }
          ]
        },
        "pageSize": {
          "description": "Number of threads per page.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "Page number to retrieve.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "edx_discussion_topics",
    "description": "Get edX/Open edX discussion topics for a course.\n\nUses `GET /api/discussion/v1/course_topics/{course_id}/`.\n\nReturns courseware topics and non-courseware topics, including topic IDs and\nthread-list URLs. Requires `EDX_ACCESS_TOKEN`, and the user behind the token\nmust have course discussion access.\n\nUse `jq` to extract only the topic names/IDs you need.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "courseId"
      ],
      "type": "object",
      "properties": {
        "courseId": {
          "description": "Course key, e.g. \"course-v1:edX+DemoX+Demo_Course\".",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "figma_export_images",
    "description": "Export up to 10 explicit Figma nodes as PNG artifacts. Requires file content read access and configured MCP_DOWNLOAD_ALLOWED_ORIGINS for render hosts. Transfers share maxBytes; null renders are marked incomplete. PNG dimensions are bounded; images are never decoded or inlined. Returns owner-scoped artifact handles for artifact_read.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "file",
        "ids"
      ],
      "properties": {
        "file": {
          "type": "string"
        },
        "ids": {
          "description": "Comma-separated explicit node IDs; at most 10 per export.",
          "type": "string"
        },
        "scale": {
          "description": "PNG render scale, 0.01 through 4; default 1.",
          "type": [
            "number",
            "null"
          ],
          "format": "double"
        },
        "version": {
          "type": [
            "string",
            "null"
          ]
        },
        "maxBytes": {
          "description": "Maximum transferred bytes, default 32 MiB, hard maximum 512 MiB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "figma_get_file",
    "description": "Read a Figma file's **document tree** (pages, frames, layers) plus its component and style metadata. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v1/files/{key}`. `file` is either the bare file key (`AbC123dEf456GhI789jKl0`) or a Figma URL (`https://www.figma.com/design/<key>/Name?node-id=1-2` — `file`, `design`, `proto`, and `board` URLs all work); the key is extracted for you. The response's `document.children[]` are the pages; `components`, `componentSets`, and `styles` map ids to metadata; `name`, `lastModified`, and `version` describe the file.\n\n**Depth is bounded on purpose.** `depth` defaults to `1` (pages only) and is capped at `4`. A full Figma document is often tens of megabytes — responses over 10 MiB are refused, and large ones are spilled to a resumable artifact — so do **not** raise `depth` to explore: start at 1 to see the pages, then call `figma_get_nodes` with the `ids` of the frames you care about to get their subtrees. `ids` (comma-separated node ids, e.g. `1:2,3:4`; a URL's `node-id=1-2` is node `1:2`) restricts the tree to those nodes and their ancestors. `version` selects a version-history entry; `branchData: true` adds `branches[]`.\n\nAuthenticates with a Figma **personal access token** (`FIGMA_TOKEN`, sent as `X-Figma-Token`) that needs the `file_content:read` scope. A 404 means the key is wrong or the file is not shared with the token owner; 429 is Figma's rate limit (not retried automatically).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `document.children[*].{id: id, name: name}` or `{name: name, pages: document.children[*].name}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://developers.figma.com/docs/rest-api/file-endpoints/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "file"
      ],
      "type": "object",
      "properties": {
        "file": {
          "description": "The Figma file: either the bare file key (e.g. \"AbC123dEf456GhI789jKl0\")\nor a Figma URL such as\n\"https://www.figma.com/design/AbC123dEf456GhI789jKl0/My-Design?node-id=1-2\".\n`file`, `design`, `proto`, and `board` URLs are accepted; the key is\nextracted from the URL.",
          "type": "string"
        },
        "depth": {
          "description": "How many levels of the document tree to return. Default 1 (pages only);\nmaximum 4. Each extra level multiplies the response size — for a deep\nsubtree use `figma_get_nodes` with explicit `ids` instead of raising\nthis.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "ids": {
          "description": "Comma-separated node ids to restrict the document tree to, e.g. \"1:2,3:4\"\n(the `node-id=1-2` in a Figma URL is node \"1:2\"). Only these nodes, their\nancestors, and their children (to `depth`) are returned.",
          "type": [
            "string",
            "null"
          ]
        },
        "version": {
          "description": "A specific version id from the file's version history. Omit for the\ncurrent version.",
          "type": [
            "string",
            "null"
          ]
        },
        "branchData": {
          "description": "Set true to include branch metadata (`branches[]`) in the response.\nDefault false.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"document.children[*].{id: id, name: name, type: type}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "figma_get_nodes",
    "description": "Read specific **nodes** (frames, components, layers) of a Figma file by id — the way to inspect one part of a design without fetching the whole document. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v1/files/{key}/nodes?ids=...`. `file` is a bare file key or a Figma URL (`https://www.figma.com/design/<key>/Name?node-id=1-2`). `ids` is required: comma-separated node ids such as `1:2,3:4` — the `node-id=1-2` in a Figma URL is node `1:2`; page and frame ids also come from `figma_get_file` (`document.children[].id`). At most 100 ids per call. The response's `nodes` object is keyed by id, each with a `document` subtree plus the `components` and `styles` it references; a node that does not exist maps to `null`.\n\n`depth` (default `1`, max `4`) is how many levels below each requested node are returned; raise it on one frame rather than on the whole file. `version` selects a version-history entry.\n\nAuthenticates with a Figma **personal access token** (`FIGMA_TOKEN`, sent as `X-Figma-Token`) that needs the `file_content:read` scope. A 404 means the key is wrong or the file is not shared with the token owner; 429 is Figma's rate limit (not retried automatically).\n\n**Typical flow:** `figma_get_file` (depth 1, find the page/frame ids) → `figma_get_nodes` with those ids (depth 2-4) → `jq` down to names, text, sizes, and fills.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `nodes.*.document.{name: name, type: type, children: children[*].{id: id, name: name, type: type}}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://developers.figma.com/docs/rest-api/file-endpoints/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "file",
        "ids"
      ],
      "type": "object",
      "properties": {
        "file": {
          "description": "The Figma file: either the bare file key (e.g. \"AbC123dEf456GhI789jKl0\")\nor a Figma URL such as\n\"https://www.figma.com/design/AbC123dEf456GhI789jKl0/My-Design?node-id=1-2\".",
          "type": "string"
        },
        "ids": {
          "description": "Comma-separated node ids to fetch, e.g. \"1:2,3:4\". Required. The\n`node-id=1-2` in a Figma URL is node \"1:2\". Ids may contain letters,\ndigits, `:`, `;` and `-`; at most 100 per call.",
          "type": "string"
        },
        "depth": {
          "description": "How many levels below each requested node to return. Default 1;\nmaximum 4. Omit for the node and its direct children.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "version": {
          "description": "A specific version id from the file's version history. Omit for the\ncurrent version.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"nodes.*.document.{name: name, type: type, children: children[*].name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "figma_list_comments",
    "description": "List the **comment threads** on a Figma file — design review feedback, questions, and their replies, with authors, timestamps, resolution state, and the node/position each comment is pinned to. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v1/files/{key}/comments`. `file` is a bare file key or a Figma URL (`https://www.figma.com/design/<key>/Name`). The response's `comments[]` entries carry `id`, `message`, `user.handle`, `created_at`, `resolved_at` (null while open), `parent_id` (set on replies), `order_id`, and `client_meta` (the pinned `node_id` / `node_offset`). Set `asMd: true` to get `message` rendered as Markdown instead of plain text.\n\nAuthenticates with a Figma **personal access token** (`FIGMA_TOKEN`, sent as `X-Figma-Token`) that needs the `file_comments:read` scope. A 404 means the key is wrong or the file is not shared with the token owner; 429 is Figma's rate limit (not retried automatically). Read-only: posting or resolving comments is not supported.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `comments[?resolved_at==null].{id: id, by: user.handle, msg: message, node: client_meta.node_id}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://developers.figma.com/docs/rest-api/comments-endpoints/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "file"
      ],
      "type": "object",
      "properties": {
        "file": {
          "description": "The Figma file: either the bare file key (e.g. \"AbC123dEf456GhI789jKl0\")\nor a Figma URL such as\n\"https://www.figma.com/design/AbC123dEf456GhI789jKl0/My-Design\".",
          "type": "string"
        },
        "asMd": {
          "description": "Set true to return comment bodies as Markdown (`message` rendered with\nFigma's rich-text formatting) instead of plain text. Default false.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"comments[?resolved_at==null].{id: id, by: user.handle, msg: message}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "figma_list_components",
    "description": "List the **components published** from a Figma file (a team library or design-system file): component keys, names, descriptions, containing frame/page, and the node id of each. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v1/files/{key}/components`. `file` is a bare file key or a Figma URL (`https://www.figma.com/design/<key>/Name`). The response's `meta.components[]` entries carry `key` (the library-wide component key), `name`, `description`, `node_id`, `containing_frame`, and `updated_at`. Only components that have been **published** to a library appear; unpublished local components are visible in `figma_get_file`'s `components` map instead.\n\nAuthenticates with a Figma **personal access token** (`FIGMA_TOKEN`, sent as `X-Figma-Token`) that needs the `library_content:read` scope (plus `file_content:read` for the file itself). A 404 means the key is wrong or the file is not shared with the token owner; 429 is Figma's rate limit (not retried automatically).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `meta.components[*].{key: key, name: name, node: node_id}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://developers.figma.com/docs/rest-api/component-endpoints/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "file"
      ],
      "type": "object",
      "properties": {
        "file": {
          "description": "The Figma file: either the bare file key (e.g. \"AbC123dEf456GhI789jKl0\")\nor a Figma URL such as\n\"https://www.figma.com/design/AbC123dEf456GhI789jKl0/My-Design\".",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"meta.components[*].{key: key, name: name, node: node_id}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_get_file",
    "description": "Read a file's text, or list a directory, in a GitHub repository at a branch, tag, or commit. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/contents/{path}?ref=…`. GitHub returns files base64-encoded; this tool decodes them, so a UTF-8 file comes back with `content` as plain text and `encoding: \"utf-8\"`. When the bytes are not UTF-8 (images, archives, compiled artifacts) `content` is dropped and `contentOmitted` explains why — only metadata (`name`, `path`, `size`, `sha`, `download_url`) is returned. GitHub does not inline files between 1 MB and 100 MB; those also come back with `contentOmitted` and their `size`. Files above 100 MB are not served by this endpoint at all. A `path` that names a directory returns the array of its entries (`name`, `path`, `type`, `size`, `sha`) without any content.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Contents: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required): from `full_name`, e.g. `octocat` / `hello-world`.\n- `path` (required): file or directory path inside the repository, e.g. `src/main.rs`. `..` segments are rejected.\n- `ref`: branch, tag, or commit SHA. Omit for the default branch.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need — `content` for just the text, or `[*].{name: name, type: type}` for a directory.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`). For source code, `outputFormat: \"json\"` with `jq: \"content\"` returns the text verbatim.\n\nAPI reference: https://docs.github.com/en/rest/repos/contents#get-repository-content\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo",
        "path"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "path": {
          "description": "Path of the file or directory inside the repository, e.g.\n\"src/main.rs\" or \"docs\". Slashes separate directories; `..` is\nrejected.",
          "type": "string"
        },
        "ref": {
          "description": "Branch name, tag, or commit SHA to read at, e.g. \"main\" or\n\"v1.2.0\". Omit for the repository's default branch.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"content\" for a file, \"[*].{name: name, type: type}\" for a\ndirectory.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_get_issue",
    "description": "Get one GitHub issue by number: title, body, state, labels, assignees, and milestone. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/issues/{number}`. The detail carries `body` (Markdown), `state`, `state_reason` (`completed|not_planned|reopened`), `labels[].name`, `assignees[].login`, `milestone.title`, `comments` (count), `closed_at`, `closed_by`, and `html_url`. Because GitHub numbers issues and pull requests from one sequence, a `number` that belongs to a pull request also resolves here and carries a `pull_request` key — use `github_get_pull_request` for its merge status and branches.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Issues: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `number` (required): the issue number shown in the UI, e.g. 42 (not the global `id`).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{title: title, state: state, body: body, labels: labels[*].name}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/issues/issues#get-an-issue\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo",
        "number"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "number": {
          "description": "Issue number as shown in the GitHub UI (not the global id), e.g. 42.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"{title: title, state: state, body: body, labels: labels[*].name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_get_job_logs",
    "description": "Download the text log for one GitHub Actions job as a bounded, owner-scoped artifact. Requires Actions read access. Redirect destinations must be configured in MCP_DOWNLOAD_ALLOWED_ORIGINS; API credentials are stripped across origins. Returns metadata and SHA-256; use artifact_read for bounded chunks.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "owner",
        "repo",
        "jobId"
      ],
      "properties": {
        "owner": {
          "type": "string"
        },
        "repo": {
          "type": "string"
        },
        "jobId": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "maxBytes": {
          "description": "Maximum transferred bytes, default 32 MiB, hard maximum 512 MiB.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_get_pull_request",
    "description": "Get one GitHub pull request by number: title, body, state, merge status, branches, and change counts. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/pulls/{number}`. Beyond the list fields, the detail carries `body`, `merged`, `mergeable`, `mergeable_state`, `merged_by`, `commits`, `additions`, `deletions`, `changed_files`, `review_comments`, `labels`, `requested_reviewers`, and `head.sha` (the commit CI ran against — match it to `head_sha` in `github_list_workflow_runs`). Follow with `github_get_pull_request_diff` for the actual changes.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Pull requests: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `number` (required): the pull request number shown in the UI, e.g. 1347 (not the global `id`).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{title: title, state: state, mergeable: mergeable, head: head.ref, base: base.ref, files: changed_files}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/pulls/pulls#get-a-pull-request\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo",
        "number"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "number": {
          "description": "Pull request number as shown in the GitHub UI (not the global id),\ne.g. 1347.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"{title: title, state: state, mergeable: mergeable, head: head.ref, base: base.ref}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_get_pull_request_diff",
    "description": "Get the files changed by a GitHub pull request, each with its unified diff `patch`. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/pulls/{number}/files`. Each entry carries `filename`, `status` (`added|removed|modified|renamed|…`), `additions`, `deletions`, `changes`, `previous_filename` for renames, and `patch` — the unified diff hunk text for that file. **GitHub omits `patch` for binary files and for very large text changes**; such entries still list the file and its counts, so a missing `patch` means \"diff not inlined\", not \"no change\". The endpoint returns at most 3,000 files per pull request in total.\n\nThis is the per-file JSON endpoint, not the raw `application/vnd.github.diff` media type, so `jq` can pick individual files and the response stays bounded per page.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Pull requests: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `number` (required): the pull request number, e.g. 1347.\n- `perPage` (1-100, default 30) and `page` (1-based). **Pagination continues via `page`**: a pull request touching more than `perPage` files needs further calls with `page: 2`, `3`, … until a short page comes back. Compare against `changed_files` from `github_get_pull_request`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{file: filename, status: status, changes: changes}` first, then `[?filename=='src/lib.rs'].patch` for one file's diff.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`). Diff text renders most faithfully with `outputFormat: \"json\"`.\n\nAPI reference: https://docs.github.com/en/rest/pulls/pulls#list-pull-requests-files\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n\n`patchesUnavailable` is true if any entry in this page has no `patch`, including binary files and omitted large changes. It does not establish whether GitHub returned every changed file.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo",
        "number"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "number": {
          "description": "Pull request number as shown in the GitHub UI, e.g. 1347.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "perPage": {
          "description": "Files per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. A pull request touching more files than\n`perPage` needs further pages; increment `page` until a short page\ncomes back.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{file: filename, status: status, patch: patch}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_list_issues",
    "description": "List issues of a GitHub repository, filtered by state, labels, assignee, creator, or update time. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/issues`. **Pull requests appear in this list too** — GitHub's issues endpoint returns both; an entry that is a pull request carries a `pull_request` key (with `url`, `html_url`, `merged_at`), a plain issue does not. Filter with `jq: \"[?!pull_request]\"` for issues only. Each entry carries `number`, `title`, `state`, `user.login`, `labels[].name`, `assignees[].login`, `comments`, `created_at`, `updated_at`, and `html_url`.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Issues: read** (and **Metadata: read**) on the repository; **Pull requests: read** is needed for the pull-request entries to be included.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `state`: `open` (default), `closed`, or `all`.\n- `labels`: comma-separated label names, e.g. `bug,urgent` (all must match).\n- `assignee`: a login, `none` for unassigned, or `*` for any assignee.\n- `creator`: login of the author.\n- `since`: ISO 8601 timestamp; only issues updated at or after it.\n- `sort` (`created|updated|comments`) and `direction` (`asc|desc`).\n- `perPage` (1-100, default 30) and `page` (1-based). Pagination is explicit: call again with the next `page`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[?!pull_request].{number: number, title: title, labels: labels[*].name}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/issues/issues#list-repository-issues\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "state": {
          "description": "Issue state: \"open\" (default), \"closed\", or \"all\".",
          "type": [
            "string",
            "null"
          ]
        },
        "labels": {
          "description": "Comma-separated label names an issue must carry, e.g. \"bug,urgent\".",
          "type": [
            "string",
            "null"
          ]
        },
        "assignee": {
          "description": "Assignee login, or \"none\" for unassigned, or \"*\" for any assignee.",
          "type": [
            "string",
            "null"
          ]
        },
        "creator": {
          "description": "Login of the user who opened the issue.",
          "type": [
            "string",
            "null"
          ]
        },
        "since": {
          "description": "Only issues updated at or after this ISO 8601 timestamp, e.g.\n\"2026-09-01T00:00:00Z\".",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort key: \"created\" (default), \"updated\", or \"comments\".",
          "type": [
            "string",
            "null"
          ]
        },
        "direction": {
          "description": "Sort direction: \"asc\" or \"desc\" (default).",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Results per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number to fetch.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[?!pull_request].{number: number, title: title, labels: labels[*].name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_list_pull_requests",
    "description": "List pull requests of a GitHub repository, filtered by state, head, or base branch. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/pulls`. Each pull request carries `number`, `title`, `state`, `draft`, `user.login`, `head.ref`, `base.ref`, `created_at`, `updated_at`, and `html_url`. Use `number` with `github_get_pull_request` for merge status and review counts, and with `github_get_pull_request_diff` for the changed files.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Pull requests: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `state`: `open` (default), `closed`, or `all`.\n- `head`: `user:branch` or `org:branch`, e.g. `octocat:feature-x`.\n- `base`: base branch name, e.g. `main`.\n- `sort` (`created|updated|popularity|long-running`) and `direction` (`asc|desc`).\n- `perPage` (1-100, default 30) and `page` (1-based). Pagination is explicit: call again with the next `page`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{number: number, title: title, author: user.login, draft: draft}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/pulls/pulls#list-pull-requests\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "state": {
          "description": "Pull request state: \"open\" (default), \"closed\", or \"all\".",
          "type": [
            "string",
            "null"
          ]
        },
        "head": {
          "description": "Filter by head branch as \"user:ref-name\" or \"organization:ref-name\",\ne.g. \"octocat:feature-x\".",
          "type": [
            "string",
            "null"
          ]
        },
        "base": {
          "description": "Filter by base branch name, e.g. \"main\".",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort key: \"created\" (default), \"updated\", \"popularity\", or\n\"long-running\".",
          "type": [
            "string",
            "null"
          ]
        },
        "direction": {
          "description": "Sort direction: \"asc\" or \"desc\" (default \"desc\" when sorting by\n\"created\", otherwise \"asc\").",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Results per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number to fetch.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{number: number, title: title, author: user.login, draft: draft}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_list_repositories",
    "description": "List GitHub repositories for an organization, a user, or the authenticated token's user. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /orgs/{owner}/repos` (`ownerType: \"org\"`, the default when `owner` is given), `GET /users/{owner}/repos` (`ownerType: \"user\"`), or `GET /user/repos` when `owner` is omitted (every repository the token can access, including private ones). Each repository carries `full_name`, `private`, `default_branch`, `archived`, `pushed_at`, and `html_url` — use `full_name` to split `owner`/`repo` for the other `github_*` tools.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`; `GITHUB_API_BASE` targets GitHub Enterprise Server (`https://ghe.example.com/api/v3`). Fine-grained tokens need **Metadata: read** on the repositories to list.\n\n**Parameters:**\n- `owner`: organization or user login. Omit for the authenticated user's repositories.\n- `ownerType`: `org` (default) or `user`.\n- `type`: repository filter — org: `all|public|private|forks|sources|member`; user: `all|owner|member`; authenticated user: `all|owner|public|private|member` (not combinable with `visibility`/`affiliation`).\n- `visibility` (`all|public|private`) and `affiliation` (`owner,collaborator,organization_member`): authenticated-user listing only; rejected when `owner` is given.\n- `sort` (`created|updated|pushed|full_name`) and `direction` (`asc|desc`).\n- `perPage` (1-100, default 30) and `page` (1-based). Pagination is explicit: call again with the next `page` until a short page comes back.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{name: full_name, private: private, default: default_branch}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/repos/repos#list-organization-repositories\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "GithubOwnerType": {
          "description": "Which kind of account `owner` names when listing repositories.",
          "oneOf": [
            {
              "description": "An organization: `GET /orgs/{owner}/repos`.",
              "type": "string",
              "const": "org"
            },
            {
              "description": "A user account: `GET /users/{owner}/repos`.",
              "type": "string",
              "const": "user"
            }
          ]
        },
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "owner": {
          "description": "Organization or user login to list repositories for, e.g. \"octocat\".\nOmit to list the repositories the token's user can access\n(`GET /user/repos`).",
          "type": [
            "string",
            "null"
          ]
        },
        "ownerType": {
          "description": "Whether `owner` is an organization (\"org\", the default) or a user\n(\"user\"). Ignored when `owner` is omitted.",
          "anyOf": [
            {
              "$ref": "#/$defs/GithubOwnerType"
            },
            {
              "type": "null"
            }
          ]
        },
        "type": {
          "description": "Repository type filter (GitHub `type`). Organizations: \"all\",\n\"public\", \"private\", \"forks\", \"sources\", \"member\". Users: \"all\",\n\"owner\", \"member\". Authenticated user: \"all\", \"owner\", \"public\",\n\"private\", \"member\" (cannot be combined with `visibility` or\n`affiliation`).",
          "type": [
            "string",
            "null"
          ]
        },
        "visibility": {
          "description": "Authenticated-user listing only (no `owner`): \"all\", \"public\", or\n\"private\".",
          "type": [
            "string",
            "null"
          ]
        },
        "affiliation": {
          "description": "Authenticated-user listing only (no `owner`): comma-separated\n\"owner\", \"collaborator\", \"organization_member\", e.g.\n\"owner,collaborator\".",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort key: \"created\", \"updated\", \"pushed\", or \"full_name\" (default).",
          "type": [
            "string",
            "null"
          ]
        },
        "direction": {
          "description": "Sort direction: \"asc\" or \"desc\". Defaults to \"asc\" for \"full_name\",\notherwise \"desc\".",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Results per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number to fetch. Increment it to continue past a full\npage.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{name: full_name, private: private, default: default_branch}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_list_workflow_jobs",
    "description": "List the jobs of one GitHub Actions workflow run, each with its steps and their conclusions — which job and which step failed. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/actions/runs/{runId}/jobs`. The response is `{\"total_count\": N, \"jobs\": [...]}`; each job carries `id`, `name`, `status`, `conclusion`, `started_at`, `completed_at`, `runner_name`, `html_url`, and `steps[]` with `name`, `number`, `status`, and `conclusion` per step. A job's `id` is the value the log endpoint needs. Jobs are never served from the local cache, so status is always current.\n\nJob **log text is not fetched by this tool**: GitHub serves logs through a 302 redirect to a short-lived signed URL on a different host, which this server does not follow yet. Open `html_url` for the log, or use the failing step name from `steps[]` to locate it.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Actions: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `runId` (required): the run `id` from `github_list_workflow_runs`, e.g. 30433642.\n- `filter`: `latest` (default) for the most recent attempt only, or `all` to include every re-run attempt.\n- `perPage` (1-100, default 30) and `page` (1-based). Pagination is explicit: call again with the next `page`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `jobs[?conclusion=='failure'].{name: name, failed: steps[?conclusion=='failure'].name}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/actions/workflow-jobs#list-jobs-for-a-workflow-run\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo",
        "runId"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "runId": {
          "description": "Workflow run id (the `id` from `github_list_workflow_runs`), e.g.\n30433642.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "filter": {
          "description": "\"latest\" (default) returns jobs from the most recent attempt only;\n\"all\" includes every re-run attempt.",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Results per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number to fetch.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"jobs[?conclusion=='failure'].{name: name, steps: steps[?conclusion=='failure'].name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "github_list_workflow_runs",
    "description": "List GitHub Actions workflow runs of a repository — the CI history — optionally for one workflow, branch, status, event, or actor. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /repos/{owner}/{repo}/actions/runs`, or `GET /repos/{owner}/{repo}/actions/workflows/{workflowId}/runs` when `workflowId` is given. The response is `{\"total_count\": N, \"workflow_runs\": [...]}`; each run carries `id` (pass it to `github_list_workflow_jobs`), `name`, `display_title`, `status` (`queued|in_progress|completed`), `conclusion` (`success|failure|cancelled|…`), `head_branch`, `head_sha`, `event`, `run_number`, `run_attempt`, `created_at`, `updated_at`, and `html_url`. Runs are never served from the local cache, so status is always current.\n\n**Typical flow for a red build:** `github_list_workflow_runs` (`status: \"failure\"`, find the run `id`) → `github_list_workflow_jobs` (which job and step failed) → `github_get_pull_request` / `github_get_pull_request_diff` for the change under test.\n\nAuthenticates with a personal access token (`GITHUB_TOKEN`) sent as `Authorization: Bearer`. Fine-grained tokens need **Actions: read** (and **Metadata: read**) on the repository.\n\n**Parameters:**\n- `owner` (required) and `repo` (required), e.g. `octocat` / `hello-world`.\n- `workflowId`: numeric workflow id or the workflow file name, e.g. `ci.yml`. Omit for runs of every workflow.\n- `branch`: e.g. `main`.\n- `status`: a status or conclusion — `completed`, `in_progress`, `queued`, `success`, `failure`, `cancelled`, `timed_out`, `action_required`, `neutral`, `skipped`, `stale`, `requested`, `waiting`, `pending`.\n- `event`: triggering event, e.g. `push`, `pull_request`, `workflow_dispatch`, `schedule`.\n- `actor`: login of the user who triggered the run.\n- `perPage` (1-100, default 30) and `page` (1-based). Pagination is explicit: call again with the next `page`; `total_count` tells you how many runs match.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `workflow_runs[*].{id: id, name: name, status: status, conclusion: conclusion, branch: head_branch}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.github.com/en/rest/actions/workflow-runs#list-workflow-runs-for-a-repository\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "owner",
        "repo"
      ],
      "type": "object",
      "properties": {
        "owner": {
          "description": "Repository owner (organization or user login), e.g. \"octocat\".",
          "type": "string"
        },
        "repo": {
          "description": "Repository name without the owner, e.g. \"hello-world\".",
          "type": "string"
        },
        "workflowId": {
          "description": "Restrict to one workflow: its numeric id or its file name in\n`.github/workflows`, e.g. \"ci.yml\". Omit for runs of every workflow.",
          "type": [
            "string",
            "null"
          ]
        },
        "branch": {
          "description": "Only runs for this branch, e.g. \"main\".",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Run status or conclusion: \"completed\", \"in_progress\", \"queued\",\n\"success\", \"failure\", \"cancelled\", \"timed_out\", \"action_required\",\n\"neutral\", \"skipped\", \"stale\", \"requested\", \"waiting\", \"pending\".",
          "type": [
            "string",
            "null"
          ]
        },
        "event": {
          "description": "Triggering event, e.g. \"push\", \"pull_request\", \"workflow_dispatch\",\n\"schedule\".",
          "type": [
            "string",
            "null"
          ]
        },
        "actor": {
          "description": "Login of the user who triggered the run.",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Results per page, 1-100 (default 30). Values above 100 are capped.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number to fetch.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"workflow_runs[*].{id: id, name: name, status: status, conclusion: conclusion, branch: head_branch}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_get_file",
    "description": "Read one **file** from a GitLab repository at an explicit ref (branch, tag, or commit SHA), returning its metadata and decoded text content. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/repository/files/{path}?ref={ref}`. GitLab returns the body base64-encoded; this tool decodes it. When the bytes are valid UTF-8 the response carries `content` as plain text with `encoding: \"utf-8\"`; otherwise `content` is dropped and `contentOmitted` explains that the file is binary or non-UTF-8, leaving `file_name`, `size`, `blob_id`, `commit_id`, and `last_commit_id`. No archive, LFS object, or attachment download happens.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, scopes `read_api` **and** `read_repository`) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id (`42`) or namespace path (`group/subgroup/project`). Find it with `gitlab_list_projects`.\n- `path` (required): file path inside the repository, e.g. `src/main.rs` or `.gitlab-ci.yml`. No leading slash, no `.`/`..` segments.\n- `ref` (required): branch, tag, or SHA, e.g. `main`. GitLab has no implicit default here — pass the project's `default_branch` if unsure.\n\n**Limits:** responses over 10 MiB are refused by the transport; a decoded file larger than the output budget is truncated and spilled to a resumable artifact (`artifact_read`). Large source files are better read with a `jq` projection or in the MR diff.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{path: file_path, size: size, content: content}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/repository_files/#get-file-from-repository\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "path",
        "ref"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "path": {
          "description": "File path inside the repository, e.g. \"src/main.rs\" or\n\".gitlab-ci.yml\". No leading slash, no `.` / `..` segments.",
          "type": "string"
        },
        "ref": {
          "description": "Branch name, tag, or commit SHA to read the file at, e.g. \"main\".\nRequired by GitLab — there is no implicit default branch.",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response, e.g.\n\"{path: file_path, size: size, content: content}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_get_issue",
    "description": "Fetch one GitLab **issue** by its project-scoped IID — title, description (Markdown), state, labels, assignees, milestone, due date, and links. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/issues/{iid}`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `iid` (required): the issue's **IID** — the number shown in the UI and URL (`#7` → `7`, `/-/issues/7`). This is NOT the global `id`; using the global id yields a 404 or the wrong issue.\n\nComments (notes) and related merge requests are separate endpoints and are not fetched here.\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `{title: title, state: state, description: description, labels: labels, assignees: assignees[*].username}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/issues/#single-project-issue\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "iid": {
          "description": "Project-scoped issue IID — the number in the UI and URL (`#7` → 7),\nNOT the global issue id.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response, e.g.\n\"{title: title, state: state, description: description, labels: labels}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_get_job_trace",
    "description": "Fetch one GitLab CI job trace through the existing bounded streaming artifact store. Calls `GET /projects/{project}/jobs/{jobId}/trace` with a `GITLAB_TOKEN` bearer credential (`read_api` scope).\n\n- `project`: numeric project ID or namespace path (`group/subgroup/project`).\n- `jobId`: positive global job ID from `gitlab_list_pipeline_jobs`.\n- `maxBytes`: encoded and decoded byte limit; default 32 MiB, maximum 512 MiB.\n\nSmall traces return plain text, including ANSI codes. Larger traces return JSON metadata with `artifactId`, size, SHA-256, expiry, `complete: true`, and bounded head/tail previews. Use `artifact_read` for resumable retrieval. A running job yields the log available at request time. Oversize or incomplete transfers fail without a success handle. Redirect destinations must satisfy the configured download origin allowlist.\n\n`jq` and `outputFormat` are reserved and do not transform the trace. Responses bypass the response cache.\n\nAPI reference: https://docs.gitlab.com/api/jobs/#get-a-log-file\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "jobId"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "jobId": {
          "description": "Global job id, as returned by `gitlab_list_pipeline_jobs` (`id`).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "maxBytes": {
          "description": "Maximum encoded and decoded trace bytes (default 32 MiB, maximum 512 MiB).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0
        },
        "jq": {
          "description": "Reserved; leave unset. Trace bytes and previews are not filtered.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Reserved; small traces are text and large trace metadata is JSON.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_get_merge_request",
    "description": "Fetch one GitLab **merge request** by its project-scoped IID — title, description, state, branches, author, reviewers, merge status, and `head_pipeline` (the latest pipeline's id and status, which leads to `gitlab_list_pipeline_jobs`). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/merge_requests/{iid}`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `iid` (required): the merge request's **IID** — the number shown in the UI and URL (`!12` → `12`, `/-/merge_requests/12`). This is NOT the global `id` returned in some payloads; using the global id yields a 404 or the wrong MR.\n\nFor the changed files use `gitlab_get_merge_request_diff`.\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `{title: title, state: state, sha: sha, pipeline: head_pipeline.status, pipelineId: head_pipeline.id, merge: detailed_merge_status}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/merge_requests/#get-single-mr\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "iid": {
          "description": "Project-scoped merge-request IID — the number in the UI and URL\n(`!12` → 12), NOT the global merge-request id.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response, e.g.\n\"{title: title, state: state, sha: sha, pipeline: head_pipeline.status}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_get_merge_request_diff",
    "description": "Fetch the **per-file diffs** of a GitLab merge request (paginated) — the unified diff for each changed file, for code review or to see what a failing pipeline is testing. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/merge_requests/{iid}/diffs`. The response is a JSON list; each entry has `old_path`, `new_path`, `new_file` / `renamed_file` / `deleted_file` flags, and `diff` (unified diff text).\n\n**Upstream truncation is explicit:** GitLab omits or empties `diff` for files it considers too big and marks the entry with `too_large: true` and/or `collapsed: true`; those flags are passed through untouched. When you see them, read the file directly with `gitlab_get_file` at the MR's `sha` instead. Generated-file entries carry `generated_file: true`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `iid` (required): the merge request's project-scoped **IID** (`!12` → `12`), not the global id.\n- `perPage` (1–100, default 20) / `page` (1-based): files per page. Advance `page` until a page comes back empty.\n\n**Limits:** a page whose rendered diffs exceed the output budget is truncated and spilled to a resumable artifact (`artifact_read`); lower `perPage` or project with `jq` to stay within budget.\n\n**IMPORTANT - Cost Optimization:** list changed files first with `jq: \"[*].{file: new_path, new: new_file, deleted: deleted_file, tooLarge: too_large}\"`, then fetch diffs page by page.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/merge_requests/#list-merge-request-diffs\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "iid"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "iid": {
          "description": "Project-scoped merge-request IID — the number in the UI and URL\n(`!12` → 12), NOT the global merge-request id.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "perPage": {
          "description": "Files per page, 1–100 (default 20). Each entry carries a full unified\ndiff, so keep this small.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response, e.g.\n\"[*].{file: new_path, added: new_file, deleted: deleted_file}\" to list\nchanged files without the diff bodies.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_list_issues",
    "description": "List a GitLab project's **issues** with the common filters — by state, label, assignee, author, or free-text search. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/issues`. Each issue carries its project-scoped `iid` (the `#7` number — pass it to `gitlab_get_issue`), `title`, `state`, `labels`, `assignees`, `author.username`, `milestone`, and `web_url`. Do not confuse `iid` with the global `id`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `state`: `opened`, `closed`, or `all` (GitLab default `all`).\n- `labels`: comma-separated label names (`None` / `Any` are special values).\n- `assigneeUsername` / `authorUsername`: e.g. `jdoe`.\n- `search`: full-text match on title and description.\n- `orderBy` (`created_at`, `updated_at`, `priority`, `due_date`, …) / `sort` (`asc`, `desc`).\n- `perPage` (1–100, default 20) / `page` (1-based).\n\n**Pagination:** offset paging; advance `page` until a page comes back empty (GitLab's `x-next-page` header is not surfaced).\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `[*].{iid: iid, title: title, state: state, labels: labels, assignee: assignees[0].username}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/issues/#list-project-issues\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "state": {
          "description": "Issue state: `opened`, `closed`, or `all` (GitLab's default is `all`).",
          "type": [
            "string",
            "null"
          ]
        },
        "labels": {
          "description": "Comma-separated label names the issue must carry, e.g. \"bug,p1\".\nUse \"None\" for unlabelled, \"Any\" for any label.",
          "type": [
            "string",
            "null"
          ]
        },
        "assigneeUsername": {
          "description": "Only issues assigned to this username, e.g. \"jdoe\".",
          "type": [
            "string",
            "null"
          ]
        },
        "authorUsername": {
          "description": "Only issues authored by this username, e.g. \"jdoe\".",
          "type": [
            "string",
            "null"
          ]
        },
        "search": {
          "description": "Full-text search over issue title and description.",
          "type": [
            "string",
            "null"
          ]
        },
        "orderBy": {
          "description": "Sort field: `created_at` (default), `updated_at`, `priority`,\n`due_date`, `relative_position`, `label_priority`, `milestone_due`,\n`popularity`, `weight`, `title`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort direction: `asc` or `desc` (default `desc`).",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{iid: iid, title: title, state: state, labels: labels}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_list_merge_requests",
    "description": "List a GitLab project's **merge requests** with the common filters — find the MR behind a branch, a failed pipeline, or an author. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/merge_requests`. Each MR carries its project-scoped `iid` (the `!12` number — pass it to `gitlab_get_merge_request` / `gitlab_get_merge_request_diff`), `title`, `state`, `source_branch`, `target_branch`, `author.username`, `sha`, and `web_url`. Do not confuse `iid` with the global `id`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `state`: `opened`, `closed`, `merged`, `locked`, or `all` (GitLab default `all`).\n- `sourceBranch` / `targetBranch`: exact branch names.\n- `labels`: comma-separated label names (`None` / `Any` are special values).\n- `authorUsername`: e.g. `jdoe`.\n- `orderBy` (`created_at`, `updated_at`, `title`, `merged_at`) / `sort` (`asc`, `desc`).\n- `perPage` (1–100, default 20) / `page` (1-based).\n\n**Pagination:** offset paging; advance `page` until a page comes back empty (GitLab's `x-next-page` header is not surfaced).\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `[*].{iid: iid, title: title, state: state, source: source_branch, author: author.username}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/merge_requests/#list-project-merge-requests\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "state": {
          "description": "Merge-request state: `opened`, `closed`, `merged`, `locked`, or `all`\n(GitLab's default is `all`).",
          "type": [
            "string",
            "null"
          ]
        },
        "sourceBranch": {
          "description": "Only merge requests from this source branch, e.g. \"feature/login\".",
          "type": [
            "string",
            "null"
          ]
        },
        "targetBranch": {
          "description": "Only merge requests targeting this branch, e.g. \"main\".",
          "type": [
            "string",
            "null"
          ]
        },
        "labels": {
          "description": "Comma-separated label names the merge request must carry, e.g.\n\"bug,backend\". Use \"None\" for unlabelled, \"Any\" for any label.",
          "type": [
            "string",
            "null"
          ]
        },
        "authorUsername": {
          "description": "Only merge requests authored by this username, e.g. \"jdoe\".",
          "type": [
            "string",
            "null"
          ]
        },
        "orderBy": {
          "description": "Sort field: `created_at` (default), `updated_at`, `title`, `merged_at`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort direction: `asc` or `desc` (default `desc`).",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{iid: iid, title: title, state: state, author: author.username}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_list_pipeline_jobs",
    "description": "List the **jobs** of one GitLab CI pipeline — name, stage, status, duration, failure reason, and the job `id` needed by `gitlab_get_job_trace`. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/pipelines/{pipelineId}/jobs`. Each job carries `id` (global — pass it as `jobId`), `name`, `stage`, `status`, `failure_reason`, `allow_failure`, `duration`, `web_url`, and `pipeline`. Responses are never served from the local cache.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `pipelineId` (required): the pipeline's global `id` from `gitlab_list_pipelines` or an MR's `head_pipeline.id`.\n- `scope`: ONE status to filter on — `created`, `pending`, `running`, `failed`, `success`, `canceled`, `skipped`, or `manual`. GitLab's repeated `scope[]` form (several statuses at once) is not exposed; call once per status or filter with `jq`. `scope: \"failed\"` is the usual first call for a red pipeline.\n- `includeRetried`: include earlier attempts of retried jobs (default false).\n- `perPage` (1–100, default 20) / `page` (1-based).\n\nBridge jobs (child/multi-project pipeline triggers) are not included; they live under `/bridges`.\n\n**Pagination:** offset paging; advance `page` until a page comes back empty.\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `[*].{id: id, name: name, stage: stage, status: status, reason: failure_reason}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/jobs/#list-pipeline-jobs\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project",
        "pipelineId"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "pipelineId": {
          "description": "Global pipeline id, as returned by `gitlab_list_pipelines` (`id`).",
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "scope": {
          "description": "One job status to filter on: `created`, `pending`, `running`, `failed`,\n`success`, `canceled`, `skipped`, `manual`. A single value only — to\ncombine statuses, call once per status or filter with `jq`.",
          "type": [
            "string",
            "null"
          ]
        },
        "includeRetried": {
          "description": "Include retried jobs (earlier attempts) in the listing. Default false.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "perPage": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{id: id, name: name, stage: stage, status: status}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_list_pipelines",
    "description": "List a GitLab project's **CI pipelines** — the first step in diagnosing a red build: find the pipeline, then its jobs (`gitlab_list_pipeline_jobs`), then the failing job's log (`gitlab_get_job_trace`). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects/{project}/pipelines`. Each pipeline carries `id` (global — pass it as `pipelineId`), `iid`, `status`, `source`, `ref`, `sha`, `web_url`, and timestamps. Responses are never served from the local cache: pipeline state changes after retries.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`.\n\n**Parameters:**\n- `project` (required): numeric project id or namespace path (`group/subgroup/project`).\n- `ref`: branch or tag, e.g. `main`.\n- `status`: `created`, `waiting_for_resource`, `preparing`, `pending`, `running`, `success`, `failed`, `canceled`, `skipped`, `manual`, `scheduled`.\n- `source`: `push`, `web`, `schedule`, `api`, `merge_request_event`, `parent_pipeline`, …\n- `sha`: only pipelines for this commit.\n- `orderBy` (`id`, `status`, `ref`, `updated_at`, `user_id`) / `sort` (`asc`, `desc`).\n- `perPage` (1–100, default 20) / `page` (1-based).\n\nFor a merge request, `gitlab_get_merge_request` returns `head_pipeline.id` directly.\n\n**Pagination:** offset paging; advance `page` until a page comes back empty (GitLab's `x-next-page` header is not surfaced).\n\n**IMPORTANT - Cost Optimization:** use `jq`, e.g. `[*].{id: id, status: status, ref: ref, sha: sha, source: source}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/pipelines/#list-project-pipelines\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "project"
      ],
      "type": "object",
      "properties": {
        "project": {
          "description": "Project: numeric id (\"42\") or namespace path (\"group/subgroup/project\").",
          "type": "string"
        },
        "ref": {
          "description": "Only pipelines for this branch or tag, e.g. \"main\".",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Pipeline status: `created`, `waiting_for_resource`, `preparing`,\n`pending`, `running`, `success`, `failed`, `canceled`, `skipped`,\n`manual`, `scheduled`.",
          "type": [
            "string",
            "null"
          ]
        },
        "source": {
          "description": "Trigger source: `push`, `web`, `trigger`, `schedule`, `api`,\n`external`, `pipeline`, `chat`, `webide`, `merge_request_event`,\n`external_pull_request_event`, `parent_pipeline`, …",
          "type": [
            "string",
            "null"
          ]
        },
        "sha": {
          "description": "Only pipelines for this commit SHA.",
          "type": [
            "string",
            "null"
          ]
        },
        "orderBy": {
          "description": "Sort field: `id` (default), `status`, `ref`, `updated_at`, `user_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort direction: `asc` or `desc` (default `desc`).",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{id: id, status: status, ref: ref, sha: sha}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "gitlab_list_projects",
    "description": "List GitLab **projects** the token can see, or the projects of one group — the starting point for finding a `project` identifier to pass to the other `gitlab_*` tools. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /projects` (or `GET /groups/{groupId}/projects` when `groupId` is set). Each project carries `id`, `path_with_namespace` (use either as `project` elsewhere), `default_branch`, `web_url`, and `last_activity_at`.\n\nAuthenticates with a GitLab access token (`GITLAB_TOKEN`, `read_api` scope) sent as `Authorization: Bearer`; `GITLAB_API_BASE` selects GitLab.com (default) or a Self-Managed instance. No per-call auth is needed.\n\n**Parameters:**\n- `search`: substring match on project name/path, e.g. `payments`.\n- `membership` (default `true`): only projects the token's user belongs to. `false` includes every visible project — on GitLab.com that is enormous, so combine with `search`. Ignored when `groupId` is set.\n- `owned`: only projects owned by the user (or the group).\n- `groupId`: numeric group id or full group path (`my-org/platform`) to list that group's direct projects instead.\n- `orderBy` / `sort`: e.g. `last_activity_at` + `desc` for the most recently active first.\n- `perPage` (1–100, default 20) / `page` (1-based).\n\n**Pagination:** offset paging. GitLab's `x-next-page` / `x-total-pages` headers are not surfaced, so advance `page` until a page comes back empty.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{id: id, path: path_with_namespace, branch: default_branch}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.gitlab.com/api/projects/#list-all-projects\n\n**Pagination response:** `{data: ..., nextPage: number | null}`. `jq` transforms the original API body before it is placed in `data`; `nextPage` remains available. Pass that number as `page` to continue. A null value means the upstream did not advertise a next page. Lists bypass the local response cache to preserve pagination headers.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "search": {
          "description": "Substring to match against project name and path, e.g. \"payments\".",
          "type": [
            "string",
            "null"
          ]
        },
        "membership": {
          "description": "Only projects the token's user is a member of. Defaults to true;\nset false to include every visible (public/internal) project, which\non GitLab.com is a very large set. Ignored when `groupId` is set.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "owned": {
          "description": "Only projects owned by the token's user (or, with `groupId`, owned by\nthe group).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "groupId": {
          "description": "Scope the listing to one group: a numeric group id (\"123\") or a full\ngroup path (\"my-org/platform\"). Switches the call to\n`GET /groups/{groupId}/projects`; subgroups' projects are not included.",
          "type": [
            "string",
            "null"
          ]
        },
        "orderBy": {
          "description": "Sort field: `id`, `name`, `path`, `created_at`, `updated_at`,\n`last_activity_at`, `star_count`. Default `created_at`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort direction: `asc` or `desc` (default `desc`).",
          "type": [
            "string",
            "null"
          ]
        },
        "perPage": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "page": {
          "description": "1-based page number. Continue with the response nextPage when present.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[*].{id: id, path: path_with_namespace, branch: default_branch}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "grafana_list_datasources",
    "description": "List the datasources configured in Grafana. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nUse this to discover the Loki datasource `uid` that `grafana_query_logs` requires. Each entry includes `id`, `uid`, `name`, and `type` (e.g. `loki`, `prometheus`).\n\nAuthenticates with a Grafana **service-account token** (`GRAFANA_TOKEN`) sent as `Authorization: Bearer`; `GRAFANA_URL` sets the base (e.g. `https://myorg.grafana.net` or `http://localhost:3000`).\n\n**Typical use — find Loki datasources:**\n- `jq`: `[?type=='loki'].{name: name, uid: uid}`\n\nThen pass the chosen `uid` to `grafana_query_logs` as `datasourceUid`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://grafana.com/docs/grafana/latest/developers/http_api/data_source/#get-all-data-sources\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "jq": {
          "description": "JMESPath expression to filter/transform the response. Example to find\nLoki datasources: `[?type=='loki'].{name: name, uid: uid}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "grafana_query_logs",
    "description": "Read logs from Grafana by running a LogQL query against a Loki datasource. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nThe upstream response is consumed with explicit encoded/decoded byte limits, including chunked and compressed responses, then normalized incrementally to atomic canonical NDJSON with a bounded preview, SHA-256 checksum, and manifest sidecar. Because the response is never materialized as one JSON value, `jq` is not applied on this streamed path; filter the downloaded artifact instead.\n\nOnly Loki's successful `streams` result shape is accepted. Matrix, vector, scalar, malformed, duplicate, or ambiguous structures fail the request; records are never skipped, coerced, rounded, or truncated. Each canonical record preserves the complete validated stream-label map, exact payload, and a non-negative base-10 nanosecond timestamp that fits in `u64`. Fractional, signed, negative, or overflowing timestamps are rejected.\n\nSource identity uses only the conventional Loki labels `service_name`, `namespace`, `job`, `app`, `container`, `pod`, `host`, `instance`, and `filename`, in that fixed order. Loki does not mandate a universal source-label set: when none is present, the source is `loki:unknown`, while the full label map is still preserved. Arbitrary tenant labels are deliberately not promoted into identity. Loki/Grafana may add unrelated envelope fields, which are ignored; canonical log normalization does not infer logs from metric result types or alternate tuple shapes.\n\nGrafana itself does not store logs — it queries a Loki backend. This tool runs your LogQL through Grafana's **datasource proxy** (`/api/datasources/proxy/uid/{uid}/loki/api/v1/query_range`), so Grafana's auth and datasource configuration stay in charge. Works the same for self-hosted Grafana and Grafana Cloud.\n\nAuthenticates with a Grafana **service-account token** (`GRAFANA_TOKEN`) sent as `Authorization: Bearer`; `GRAFANA_URL` sets the base (e.g. `https://myorg.grafana.net` or `http://localhost:3000`). No per-call auth is needed.\n\n**You need the Loki datasource `uid`.** Discover it with `grafana_list_datasources` (look for an entry with `type: \"loki\"` and copy its `uid`), then pass it as `datasourceUid`.\n\n**IMPORTANT - Cost Optimization:**\n- Set a small `limit` (default 100) and a tight time range (`start`/`end`).\n- Use `jq` to keep only the fields you need from the response.\n\n**LogQL examples:**\n- Log lines: `{app=\"api\"} |= \"error\"`\n- Filter out noise: `{namespace=\"prod\"} != \"healthcheck\"`\n- Metric over time: `sum by (level) (count_over_time({app=\"api\"}[5m]))`\n\n**Parameters:**\n- `start` / `end`: RFC3339 (`2024-01-01T00:00:00Z`) or Unix nanoseconds. Omit to use Loki's defaults (last hour → now).\n- `limit`: max log lines (default 100).\n- `direction`: `backward` (newest first, default) or `forward`.\n- `step`: resolution for metric queries (e.g. `30s`); ignored for plain log selectors.\n\n**Response shape:** Loki returns `{ \"status\": \"success\", \"data\": { \"resultType\": \"streams\"|\"matrix\", \"result\": [ { \"stream\": {labels}, \"values\": [[ts, line], …] } ] } }`. A bad LogQL query comes back as an HTTP error with a `{\"error\": …}` message.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `data.result[*].values`, `data.result[*].{labels: stream, lines: values}`\n\nAPI reference: https://grafana.com/docs/loki/latest/reference/loki-http-api/#query-logs-within-a-range-of-time\n\n`timePartitions` (2-16) is an opt-in preview of safe partition ingestion. It activates only when both bounds are exact RFC3339 or integer Unix-nanosecond instants and an explicit global `limit` is present. Loki documents `start` as inclusive and `end` as exclusive, so each request maps directly to an adjacent client interval `[start,end)` without rounding. Partitions are processed in the requested direction with a decreasing remaining limit. Until bounded merge/deduplication lands, the returned artifact is a conservative `partial` partition-set status that names atomic, checksummed canonical parts; it is not a final combined log artifact. Relative, missing, fractional numeric, invalid, reversed, or otherwise ambiguous bounds use the unchanged single-request path.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "datasourceUid",
        "query"
      ],
      "type": "object",
      "properties": {
        "datasourceUid": {
          "description": "UID of the Loki datasource to query. Find it via\n`grafana_list_datasources` (the `uid` field of a `type: \"loki\"` entry).",
          "type": "string"
        },
        "query": {
          "description": "LogQL query, e.g. `{app=\"api\"} |= \"error\"` or\n`sum by (level) (count_over_time({app=\"api\"}[5m]))`.",
          "type": "string"
        },
        "start": {
          "description": "Range start: RFC3339 (`2024-01-01T00:00:00Z`) or Unix nanoseconds.\nDefaults to Loki's default window (last hour) when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "end": {
          "description": "Range end: RFC3339 or Unix nanoseconds. Defaults to \"now\" when omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "timePartitions": {
          "description": "Split an absolute range into this many requests (2-16). Until the\npartition merge slice lands, the returned artifact is a partition-set\nmanifest whose entries identify the retained canonical NDJSON parts.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0,
          "maximum": 255
        },
        "limit": {
          "description": "Max number of log lines to return (Loki defaults to 100). Keep this small\nto reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "direction": {
          "description": "Scan direction: `backward` (newest first, default) or `forward`.",
          "type": [
            "string",
            "null"
          ]
        },
        "step": {
          "description": "Query resolution step for metric queries, e.g. `30s` or `1m`. Only\nmeaningful for LogQL metric queries; ignored for log selectors.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data.result[*].values\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_delete",
    "description": "Delete Jira resources. Returns TOON format by default.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Delete issue:** `/rest/api/3/issue/{issueIdOrKey}`\n   Query param: `deleteSubtasks=true` to delete subtasks\n\n2. **Delete comment:** `/rest/api/3/issue/{issueIdOrKey}/comment/{commentId}`\n\n3. **Delete worklog:** `/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}`\n\n4. **Delete attachment:** `/rest/api/3/attachment/{attachmentId}`\n\n5. **Remove watcher:** `/rest/api/3/issue/{issueIdOrKey}/watchers`\n   Query param: `accountId={accountId}`\n\nNote: Most DELETE endpoints return 204 No Content on success.\n\nAPI reference: https://developer.atlassian.com/cloud/jira/platform/rest/v3/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_get",
    "description": "Read any Jira data. Returns TOON format by default (30-60% fewer tokens than JSON).\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` param to filter response fields. Unfiltered responses are very expensive!\n- Use `maxResults` query param to restrict result count (e.g., `maxResults: \"5\"`)\n- If unsure about available fields, first fetch ONE item with `maxResults: \"1\"` and NO jq filter to explore the schema, then use jq in subsequent calls\n\n**Schema Discovery Pattern:**\n1. First call: `path: \"/rest/api/3/search/jql\", queryParams: {\"maxResults\": \"1\", \"jql\": \"project=PROJ\"}` (no jq) - explore available fields\n2. Then use: `jq: \"issues[*].{key: key, summary: fields.summary, status: fields.status.name}\"` - extract only what you need\n\n**Output format:** TOON (default, token-efficient) or JSON (`outputFormat: \"json\"`)\n\n**Common paths:**\n- `/rest/api/3/project` - list all projects\n- `/rest/api/3/project/{projectKeyOrId}` - get project details\n- `/rest/api/3/search/jql` - search issues with JQL (use `jql` query param). NOTE: `/rest/api/3/search` is deprecated!\n- `/rest/api/3/issue/{issueIdOrKey}` - get issue details\n- `/rest/api/3/issue/{issueIdOrKey}/comment` - list issue comments\n- `/rest/api/3/issue/{issueIdOrKey}/worklog` - list issue worklogs\n- `/rest/api/3/issue/{issueIdOrKey}/transitions` - get available transitions\n- `/rest/api/3/user/search` - search users (use `query` param)\n- `/rest/api/3/status` - list all statuses\n- `/rest/api/3/issuetype` - list issue types\n- `/rest/api/3/priority` - list priorities\n\n**JQ examples:** `issues[*].key`, `issues[0]`, `issues[*].{key: key, summary: fields.summary}`\n\n**Example JQL queries:** `project=PROJ`, `assignee=currentUser()`, `status=\"In Progress\"`, `created >= -7d`\n\nAPI reference: https://developer.atlassian.com/cloud/jira/platform/rest/v3/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_get_attachment",
    "description": "Download a Jira image attachment and return an MCP image block the assistant can view. Preserves the original bytes; no text or JSON conversion. Supports PNG, JPEG, GIF, and WebP up to 5 MiB.\n\nFirst use jira_get with path `/rest/api/3/issue/{issueKey}`, queryParams `{\"fields\":\"attachment\"}`, and jq `fields.attachment[*].{id:id,filename:filename,mimeType:mimeType,size:size}`. Pass the selected numeric ID as attachmentId.\n\nUse this tool for screenshot contents instead of jira_get. Media UUIDs and blob URLs from rendered comments are not Jira attachment IDs. Non-image attachments return an error.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "required": [
        "attachmentId"
      ],
      "properties": {
        "attachmentId": {
          "description": "Numeric Jira attachment ID from fields.attachment, not a media UUID or blob URL.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_patch",
    "description": "Partially update Jira resources. Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:** Use `jq` param to filter response fields.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update issue fields:** `/rest/api/3/issue/{issueIdOrKey}`\n   body: `{\"fields\": {\"summary\": \"Updated title\"}}` (only updates specified fields)\n\n2. **Update comment:** `/rest/api/3/issue/{issueIdOrKey}/comment/{commentId}`\n   body: `{\"body\": {\"type\": \"doc\", \"version\": 1, \"content\": [{\"type\": \"paragraph\", \"content\": [{\"type\": \"text\", \"text\": \"Updated comment\"}]}]}}`\n\n3. **Update worklog:** `/rest/api/3/issue/{issueIdOrKey}/worklog/{worklogId}`\n   body: `{\"timeSpentSeconds\": 7200}`\n\nNote: PATCH only updates the fields you specify, leaving others unchanged.\n\nAPI reference: https://developer.atlassian.com/cloud/jira/platform/rest/v3/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_post",
    "description": "Create Jira resources. Returns TOON format by default (token-efficient).\n\n**IMPORTANT - Cost Optimization:**\n- Use `jq` param to extract only needed fields from response (e.g., `jq: \"{key: key, id: id}\"`)\n- Unfiltered responses include all metadata and are expensive!\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Create issue:** `/rest/api/3/issue`\n   body: `{\"fields\": {\"project\": {\"key\": \"PROJ\"}, \"summary\": \"Issue title\", \"issuetype\": {\"name\": \"Task\"}, \"description\": {\"type\": \"doc\", \"version\": 1, \"content\": [{\"type\": \"paragraph\", \"content\": [{\"type\": \"text\", \"text\": \"Description\"}]}]}}}`\n\n2. **Add comment:** `/rest/api/3/issue/{issueIdOrKey}/comment`\n   body: `{\"body\": {\"type\": \"doc\", \"version\": 1, \"content\": [{\"type\": \"paragraph\", \"content\": [{\"type\": \"text\", \"text\": \"Comment text\"}]}]}}`\n\n3. **Add worklog:** `/rest/api/3/issue/{issueIdOrKey}/worklog`\n   body: `{\"timeSpentSeconds\": 3600, \"comment\": {\"type\": \"doc\", \"version\": 1, \"content\": [{\"type\": \"paragraph\", \"content\": [{\"type\": \"text\", \"text\": \"Work done\"}]}]}}`\n\n4. **Transition issue:** `/rest/api/3/issue/{issueIdOrKey}/transitions`\n   body: `{\"transition\": {\"id\": \"31\"}}`\n\n5. **Add attachment:** `/rest/api/3/issue/{issueIdOrKey}/attachments`\n   Note: Requires multipart form data (complex - use Jira UI for attachments)\n\nAPI reference: https://developer.atlassian.com/cloud/jira/platform/rest/v3/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "jira_put",
    "description": "Replace Jira resources (full update). Returns TOON format by default.\n\n**IMPORTANT - Cost Optimization:** Use `jq` param to extract only needed fields from response\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`)\n\n**Common operations:**\n\n1. **Update issue (full):** `/rest/api/3/issue/{issueIdOrKey}`\n   body: `{\"fields\": {\"summary\": \"New title\", \"description\": {...}, \"assignee\": {\"accountId\": \"...\"}}}`\n\n2. **Update project:** `/rest/api/3/project/{projectIdOrKey}`\n   body: `{\"name\": \"New Project Name\", \"description\": \"Updated description\"}`\n\n3. **Set issue property:** `/rest/api/3/issue/{issueIdOrKey}/properties/{propertyKey}`\n   body: `{\"value\": \"property value\"}`\n\nNote: PUT replaces the entire resource. For partial updates, prefer PATCH.\n\nAPI reference: https://developer.atlassian.com/cloud/jira/platform/rest/v3/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "mend_get_project",
    "description": "Get one Mend **project** by UUID — its name, the application (former \"product\") it belongs to, and its last-scan metadata. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `POST /api/v3.0/orgs/{orgUuid}/projects/summaries` with `projectUuids: [projectUuid]`. Find the UUID with `mend_list_projects`.\n\nAuthenticates by logging in with `MEND_EMAIL` / `MEND_USER_KEY` / `MEND_ORG_UUID` at `POST /api/v3.0/login` and exchanging its refresh token at `POST /api/v3.0/login/accessToken` for an organization-scoped JWT (sent as `Authorization: Bearer`, refreshed automatically). No per-call auth is needed.\n\n**Parameters:**\n- `projectUuid` (required): the project UUID, e.g. `7f2c9e10-3b4d-4e5f-a6b7-c8d9e0f1a2b3`.\n\n**Response envelope:** Mend wraps the project as `{\"response\": [...], \"additionalData\": {...}, \"supportToken\": ...}` — the fields are under `response`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `response[0].{name: name, lastScan: lastScan}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.mend.io/platform/latest/getting-started-with-mend-api-3-0\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "projectUuid"
      ],
      "type": "object",
      "properties": {
        "projectUuid": {
          "description": "Project UUID as returned by `mend_list_projects`, e.g.\n`7f2c9e10-3b4d-4e5f-a6b7-c8d9e0f1a2b3`.",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: `response.{name: name, lastScan: lastScan}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "mend_list_applications",
    "description": "List the **applications** of the configured Mend organization. In Mend Platform API 3.0 \"applications\" are the former WhiteSource \"products\" — the grouping one level above projects. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/v3.0/orgs/{orgUuid}/applications`. The organization comes from `MEND_ORG_UUID` in config — it is not an argument. Each application carries its `uuid` and `name`; pass the `uuid` to `mend_list_projects` as `applicationUuid`.\n\nAuthenticates by logging in with `MEND_EMAIL` / `MEND_USER_KEY` / `MEND_ORG_UUID` at `POST /api/v3.0/login` and exchanging its refresh token at `POST /api/v3.0/login/accessToken` for an organization-scoped JWT (sent as `Authorization: Bearer`, refreshed automatically). No per-call auth is needed.\n\n**Parameters:**\n- `search`: free-text filter on the application name, e.g. `payments`.\n- `limit`: page size, 1..=200 (default 50).\n- `cursor`: opaque page cursor from a previous response's `additionalData.cursor`. Omit for the first page.\n\n**Response envelope:** Mend wraps results as `{\"response\": [...], \"additionalData\": {\"cursor\": ..., ...}, \"supportToken\": ...}`. The items are in `response[]`; when `additionalData.cursor` is present pass it back as `cursor` for the next page. Results are not aggregated across pages.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `response[*].{uuid: uuid, name: name}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.mend.io/platform/latest/getting-started-with-mend-api-3-0\n\nOptional search, severity, and status filters (where exposed) apply locally to the requested page before `jq`. An empty filtered page does not imply the cursor is exhausted; preserve `additionalData.cursor` when choosing a projection.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "cursor": {
          "description": "Opaque page cursor from a previous response's `additionalData.cursor`.\nOmit for the first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1..=200 (default 50). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "search": {
          "description": "Free-text filter on the application name, e.g. `payments`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: `response[*].{uuid: uuid, name: name}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "mend_list_findings",
    "description": "List a Mend project's **SCA security findings** — the vulnerable open-source dependencies (CVE, severity, affected library and version, fix availability). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/v3.0/projects/{projectUuid}/dependencies/findings/security`. This is the dependency (SCA) finding set only; SAST, container and license findings are not covered by this tool.\n\nAuthenticates by logging in with `MEND_EMAIL` / `MEND_USER_KEY` / `MEND_ORG_UUID` at `POST /api/v3.0/login` and exchanging its refresh token at `POST /api/v3.0/login/accessToken` for an organization-scoped JWT (sent as `Authorization: Bearer`, refreshed automatically). No per-call auth is needed.\n\n**Parameters:**\n- `projectUuid` (required): the project UUID from `mend_list_projects`.\n- `severity`: comma-separated `critical`, `high`, `medium`, `low` (case-insensitive), e.g. `critical,high`. Omit for all.\n- `status`: Local filter on the returned finding status (e.g. `ACTIVE`). Omit for all.\n- `limit`: page size, 1..=200 (default 50).\n- `cursor`: opaque page cursor from a previous response's `additionalData.cursor`. Omit for the first page.\n\n**Response envelope:** Mend wraps results as `{\"response\": [...], \"additionalData\": {\"cursor\": ..., ...}, \"supportToken\": ...}`. The finding fields come from the items in `response[]` and the totals from `additionalData`; when `additionalData.cursor` is present pass it back as `cursor` for the next page. Results are not aggregated across pages.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `response[*].{cve: vulnerability.name, severity: vulnerability.severity, lib: component.name, fix: topFix.fixResolution}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.mend.io/platform/latest/getting-started-with-mend-api-3-0\n\nOptional search, severity, and status filters (where exposed) apply locally to the requested page before `jq`. An empty filtered page does not imply the cursor is exhausted; preserve `additionalData.cursor` when choosing a projection.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "projectUuid"
      ],
      "type": "object",
      "properties": {
        "projectUuid": {
          "description": "Project UUID as returned by `mend_list_projects`, e.g.\n`7f2c9e10-3b4d-4e5f-a6b7-c8d9e0f1a2b3`.",
          "type": "string"
        },
        "severity": {
          "description": "Local page filter: comma-separated severities to include: `critical`, `high`, `medium`,\n`low` (case-insensitive), e.g. `critical,high`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Local status filter on the returned page, e.g. `ACTIVE`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor": {
          "description": "Opaque page cursor from a previous response's `additionalData.cursor`.\nOmit for the first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1..=200 (default 50). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: `response[*].{cve: vulnerability.name, severity: vulnerability.severity, lib: component.name}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "mend_list_projects",
    "description": "List Mend **projects** — the scanned units (a repository, a build, a container image) that carry findings. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/v3.0/orgs/{orgUuid}/projects` for the whole organization (`MEND_ORG_UUID` from config), or `POST /api/v3.0/orgs/{orgUuid}/projects/summaries` with `applicationUuids: [applicationUuid]` when `applicationUuid` is given. Each project carries its `uuid` and `name`; pass the `uuid` to `mend_get_project` or `mend_list_findings` as `projectUuid`.\n\nAuthenticates by logging in with `MEND_EMAIL` / `MEND_USER_KEY` / `MEND_ORG_UUID` at `POST /api/v3.0/login` and exchanging its refresh token at `POST /api/v3.0/login/accessToken` for an organization-scoped JWT (sent as `Authorization: Bearer`, refreshed automatically). No per-call auth is needed.\n\n**Parameters:**\n- `applicationUuid`: restrict to one application (former \"product\"), from `mend_list_applications`. Omit for every project in the organization.\n- `search`: free-text filter on the project name, e.g. `api-gateway`.\n- `limit`: page size, 1..=200 (default 50).\n- `cursor`: opaque page cursor from a previous response's `additionalData.cursor`. Omit for the first page.\n\n**Response envelope:** Mend wraps results as `{\"response\": [...], \"additionalData\": {\"cursor\": ..., ...}, \"supportToken\": ...}`. The items are in `response[]`; when `additionalData.cursor` is present pass it back as `cursor` for the next page. Results are not aggregated across pages.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `response[*].{uuid: uuid, name: name, lastScan: lastScan}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.mend.io/platform/latest/getting-started-with-mend-api-3-0\n\nOptional search, severity, and status filters (where exposed) apply locally to the requested page before `jq`. An empty filtered page does not imply the cursor is exhausted; preserve `additionalData.cursor` when choosing a projection.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "applicationUuid": {
          "description": "Restrict to the projects of one application (former \"product\"), by\nits UUID as returned by `mend_list_applications`, e.g.\n`4c5a1b2e-9d3f-4a6b-8c7d-0e1f2a3b4c5d`. Omit to list every project in\nthe organization.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor": {
          "description": "Opaque page cursor from a previous response's `additionalData.cursor`.\nOmit for the first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1..=200 (default 50). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "search": {
          "description": "Free-text filter on the project name, e.g. `api-gateway`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: `response[*].{uuid: uuid, name: name, lastScan: lastScan}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "newrelic_query",
    "description": "Query New Relic via the NerdGraph GraphQL API. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nNerdGraph is New Relic's single GraphQL endpoint — NRQL queries, entity search, dashboards, alerts, and account data are all driven through it. This one tool POSTs your GraphQL document (plus optional `variables`) to `/graphql`.\n\nAuthenticates with a New Relic **User API key** (`NEW_RELIC_API_KEY`) sent in the `API-Key` header; no per-call auth is needed. EU-region accounts must set `NEW_RELIC_REGION=eu`.\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS request only the fields you need in the GraphQL selection set, and use `jq` to trim the response further.\n- To explore, request a small selection first, then widen it on later calls.\n\n**NerdGraph quirks you must know:**\n- A request can **fail with HTTP 200** and a top-level `errors` array (query syntax, NRQL, or permission failures). This tool reclassifies a non-empty `errors` array as an error automatically, so a successful result has no `errors`.\n- You need the numeric **account id** for most queries. Find it with: `{ actor { accounts { id name } } }`.\n\n**Running NRQL** (the common case) — wrap the NRQL string in a NerdGraph query:\n```graphql\n{ actor { account(id: 1234567) { nrql(query: \"SELECT count(*) FROM Transaction SINCE 1 hour ago\") { results } } } }\n```\nPrefer `variables` for the account id and NRQL string instead of string-interpolating them:\n- query: `query($id: Int!, $q: Nrql!) { actor { account(id: $id) { nrql(query: $q) { results } } } }`\n- variables: `{\"id\": 1234567, \"q\": \"SELECT average(duration) FROM Transaction TIMESERIES SINCE 1 day ago\"}`\n\n**Other common queries:**\n- List accounts: `{ actor { accounts { id name } } }`\n- Entity search: `{ actor { entitySearch(query: \"domain = 'APM' AND name LIKE '%api%'\") { results { entities { guid name entityType } } } } }`\n- Current user: `{ actor { user { name email } } }`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `data.actor.account.nrql.results`, `data.actor.entitySearch.results.entities[*].{guid: guid, name: name}`\n\nAPI reference: https://docs.newrelic.com/docs/apis/nerdgraph/get-started/introduction-new-relic-nerdgraph/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "query"
      ],
      "type": "object",
      "properties": {
        "query": {
          "description": "NerdGraph GraphQL document to execute. NRQL queries are run by wrapping\nthem here, e.g.\n`{ actor { account(id: 123) { nrql(query: \"SELECT count(*) FROM Transaction SINCE 1 hour ago\") { results } } } }`.",
          "type": "string"
        },
        "variables": {
          "description": "Optional GraphQL variables object, referenced by `$name` in `query`.\nExample: `{\"id\": 1234567, \"q\": \"SELECT average(duration) FROM Transaction\"}`."
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data.actor.account.nrql.results\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_db_query_central",
    "description": "Run one read-only SQL query against the central database of an allowlisted\nNinjaOne QA/dev environment.\n\nOnly a single `SELECT` or `WITH ... SELECT` is accepted. The connection target\ncomes exclusively from `NINJAONE_DB_ENVIRONMENTS`; production and arbitrary\nhosts are refused. The session is forced read-only, has a 10-second statement\ntimeout, and limits the returned row count. Select only the columns and rows\nneeded, especially when tables may contain customer data or credentials.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "environment",
        "sql"
      ],
      "type": "object",
      "properties": {
        "environment": {
          "description": "Fixed QA/dev alias from `NINJAONE_DB_ENVIRONMENTS`.",
          "type": "string"
        },
        "sql": {
          "description": "One read-only SELECT or WITH ... SELECT statement against `centraldb`.",
          "type": "string"
        },
        "rowLimit": {
          "description": "Maximum rows returned. Defaults to 500 and is capped at 10,000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "Optional JMESPath expression to filter/transform the result.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_db_query_division",
    "description": "Run one read-only SQL query against a NinjaOne division database in an\nallowlisted QA/dev environment.\n\nPass `dbHost` and `dbName` from `ninjaone_db_resolve_division`. `dbHost` is resolved through\nthe selected environment's fixed host allowlist; it is never treated as a raw\nnetwork hostname. If the resolved `dbHost` is null, omit it: the query uses the\nconfigured host automatically when exactly one exists, and otherwise asks for\nan explicit host key. Only a single `SELECT` or `WITH ... SELECT` is accepted. The\nsession is forced read-only, has a 10-second statement timeout, and limits the\nreturned row count. Use a narrow projection and WHERE clause to avoid exposing\nunnecessary customer data.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "environment",
        "dbName",
        "sql"
      ],
      "type": "object",
      "properties": {
        "environment": {
          "description": "Fixed QA/dev alias from `NINJAONE_DB_ENVIRONMENTS`.",
          "type": "string"
        },
        "dbHost": {
          "description": "`db_host` key returned by `resolve_division`. It must map to a connection\nin the selected environment's `divisionHosts` allowlist. May be omitted\nwhen the environment configures exactly one division host.",
          "type": [
            "string",
            "null"
          ]
        },
        "dbName": {
          "description": "Physical `div_<company>_<suffix>` database name returned by\n`resolve_division`.",
          "type": "string"
        },
        "sql": {
          "description": "One read-only SELECT or WITH ... SELECT statement.",
          "type": "string"
        },
        "rowLimit": {
          "description": "Maximum rows returned. Defaults to 500 and is capped at 10,000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "Optional JMESPath expression to filter/transform the result.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_db_resolve_division",
    "description": "Resolve a NinjaOne division in an allowlisted QA/dev environment.\n\nUse the `divisionUid` from `/ws/webapp/sessionproperties`, an exact `div_...`\ndatabase name, a `db_host`, or a partial database/hostname fragment. This tool\nqueries only that environment's central `division` table and returns the\nphysical database metadata needed by `ninjaone_db_query_division`. It never connects to\nthe division database. Production and arbitrary hosts are not supported.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "environment",
        "division"
      ],
      "type": "object",
      "properties": {
        "environment": {
          "description": "Fixed QA/dev alias from `NINJAONE_DB_ENVIRONMENTS`, e.g. `qa5` or\n`dev-backup2`. Arbitrary hosts and production aliases are rejected.",
          "type": "string"
        },
        "division": {
          "description": "Division UID, exact `div_...` database name, `db_host`, or a partial\ndatabase/hostname fragment.",
          "type": "string"
        },
        "jq": {
          "description": "Optional JMESPath expression to filter/transform matching rows.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_delete",
    "description": "DELETE a resource through a NinjaOne API endpoint on a configured server. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. This is destructive: verify the server, path, resource ID, and query parameters before calling. The MCP server adds authentication.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "API path relative to the selected NinjaOne server. Public API examples\nuse `/v2/devices`; private console endpoints use `/ws/...`.",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_get",
    "description": "Read any NinjaOne API endpoint on a configured server. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. The MCP server adds its configured authentication; never include tokens or cookies in arguments. Prefer public `/v2/...` endpoints. Private `/ws/...` console endpoints may change without notice.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "API path relative to the selected NinjaOne server. Public API examples\nuse `/v2/devices`; private console endpoints use `/ws/...`.",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_login",
    "description": "Log in to a configured NinjaOne server and cache a console session key for the other `ninjaone_*` tools. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. The email and password are server-held configuration (`NINJAONE_EMAIL` / `NINJAONE_PASSWORD`, or the `email` / `password` fields of the chosen `NINJAONE_SERVERS` entry) and are never accepted as arguments. Each server can therefore log in as a different account, which is what decides the division and role the session sees; the response reports which account and division it got. When the account has MFA enabled, the current one-time code comes from the server-configured `NINJAONE_TOTP_COMMAND` or `NINJAONE_TOTP_SECRET` if either is set; otherwise pass it as `mfaCode`. The session key is kept in memory for the life of the MCP server process, replayed as the `sessionKey` cookie on later calls, and is never returned in full — call this once per server process for each server you use, and again if a later call reports the session expired.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "mfaCode": {
          "description": "Current multi-factor code for the configured account (6 digits for a\nTOTP authenticator). Required whenever the account has MFA enabled.",
          "type": [
            "string",
            "null"
          ]
        },
        "recaptchaToken": {
          "description": "reCAPTCHA token, only when the tenant reports `recaptchaRequired`.\nNormally omitted.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the login summary.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_patch",
    "description": "PATCH JSON on a NinjaOne API endpoint on a configured server. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. Verify the server, resource ID, and requested changes before calling. The MCP server adds authentication.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "API path relative to the selected NinjaOne server.",
          "type": "string"
        },
        "body": {
          "description": "JSON request body expected by the selected endpoint."
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_post",
    "description": "POST JSON to a NinjaOne API endpoint on a configured server. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. This can perform consequential actions such as running scripts or deleting devices through action endpoints; verify the server, path, IDs, and body before calling. The MCP server adds authentication.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "API path relative to the selected NinjaOne server.",
          "type": "string"
        },
        "body": {
          "description": "JSON request body expected by the selected endpoint."
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "ninjaone_put",
    "description": "PUT JSON to a NinjaOne API endpoint on a configured server. Pass `server` as an alias from `NINJAONE_SERVERS`, or omit it to use `NINJAONE_URL`. Verify the server, resource ID, and complete replacement body before calling. The MCP server adds authentication.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "server": {
          "description": "Optional alias from the `NINJAONE_SERVERS` JSON object. When omitted,\n`NINJAONE_URL` is used. Raw URLs are intentionally not accepted here.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "API path relative to the selected NinjaOne server.",
          "type": "string"
        },
        "body": {
          "description": "JSON request body expected by the selected endpoint."
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "Optional JMESPath expression to reduce or reshape the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "postman_delete",
    "description": "Delete a Postman resource via the Postman API (HTTP DELETE). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a Postman API key (`POSTMAN_API_KEY`) sent in the `X-API-Key` header; no per-call auth is needed.\n\n**DESTRUCTIVE — deletion is permanent.** Confirm the `uid`/`id` before calling.\n\n**Common DELETE endpoints:**\n- `/collections/{uid}` - delete a collection\n- `/environments/{uid}` - delete an environment\n- `/workspaces/{id}` - delete a workspace\n- `/mocks/{id}`, `/monitors/{id}` - delete a mock server / monitor\n\nThe path identifies the resource; no body is sent. The response typically echoes the deleted resource's id.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://learning.postman.com/docs/developer/postman-api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "postman_get",
    "description": "Read any Postman data via the Postman API (GET). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a Postman API key (`POSTMAN_API_KEY`) sent in the `X-API-Key` header; no per-call auth is needed.\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` to filter response fields. Collection and workspace payloads are large.\n- To explore a schema, fetch ONE item (e.g. a single collection) with NO jq, then add a jq filter on later calls.\n\n**Common GET endpoints:**\n- `/me` - the authenticated user and plan\n- `/workspaces` - list workspaces; `/workspaces/{id}` for one (includes its collections/environments)\n- `/collections` - list collections (each has a `uid`); `/collections/{uid}` for the full collection\n- `/environments` - list environments; `/environments/{uid}` for one\n- `/mocks`, `/monitors` - mock servers and monitors\n- `/apis` - APIs (Postman API builder)\n\n**IDs:** most item endpoints take a `uid` of the form `{ownerId}-{guid}`, returned in the list responses.\n\n**Pagination:** some collection endpoints accept `limit` and `offset` query params; otherwise responses are returned whole.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `collections[*].{uid: uid, name: name}`, `workspaces[*].name`, `environments[*].id`\n\nAPI reference: https://learning.postman.com/docs/developer/postman-api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "postman_patch",
    "description": "Partially update a Postman resource via the Postman API (HTTP PATCH). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a Postman API key (`POSTMAN_API_KEY`) sent in the `X-API-Key` header; no per-call auth is needed.\n\nPATCH applies a partial update — send only the fields you want to change. To replace a whole resource use `postman_put`.\n\n**Common PATCH endpoints:**\n- `/workspaces/{id}` - rename or re-describe a workspace. Body: `{\"workspace\": {\"name\": \"New name\"}}`.\n- `/collections/{uid}` - some collection metadata updates are accepted as PATCH.\n\n**Body:** pass the partial JSON request body as `body`, wrapped in the resource's top-level key.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://learning.postman.com/docs/developer/postman-api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "postman_post",
    "description": "Create Postman resources via the Postman API (HTTP POST). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a Postman API key (`POSTMAN_API_KEY`) sent in the `X-API-Key` header; no per-call auth is needed.\n\n**Common POST endpoints:**\n- `/collections` - create a collection. Body: `{\"collection\": { ...collection schema... }}`. Add `?workspace={id}` to place it in a workspace.\n- `/environments` - create an environment. Body: `{\"environment\": {\"name\": \"Prod\", \"values\": [...]}}`.\n- `/workspaces` - create a workspace. Body: `{\"workspace\": {\"name\": \"Team\", \"type\": \"team\"}}`.\n- `/mocks`, `/monitors` - create a mock server / monitor.\n- `/import/openapi` - import an OpenAPI spec into a collection.\n\n**Body:** pass the JSON request body as `body`. Postman wraps most resources in a top-level key (`collection`, `environment`, `workspace`).\n\n**Query params:** several creates accept `?workspace={id}` to target a workspace.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://learning.postman.com/docs/developer/postman-api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "postman_put",
    "description": "Replace a Postman resource via the Postman API (HTTP PUT). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a Postman API key (`POSTMAN_API_KEY`) sent in the `X-API-Key` header; no per-call auth is needed.\n\nPUT replaces the **entire** resource — send the full object, not a partial. For partial edits use `postman_patch` where the endpoint supports it.\n\n**Common PUT endpoints:**\n- `/collections/{uid}` - replace a collection. Body: `{\"collection\": { ...full collection schema... }}`.\n- `/environments/{uid}` - replace an environment. Body: `{\"environment\": {\"name\": \"...\", \"values\": [...]}}`.\n- `/mocks/{id}`, `/monitors/{id}` - replace a mock server / monitor.\n\n**Body:** pass the full JSON request body as `body`, wrapped in the resource's top-level key.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://learning.postman.com/docs/developer/postman-api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sentry_get_event",
    "description": "Get one **event** of a Sentry issue — the concrete occurrence with its exception/stack trace, breadcrumbs, request, tags, and runtime contexts. This is the \"show me the actual crash\" call. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/0/organizations/{organization}/issues/{issueId}/events/{eventId}/`.\n\nAuthenticates with a Sentry **auth token** (`SENTRY_TOKEN`, scopes `org:read` + `project:read` + `event:read`) sent as `Authorization: Bearer`; `SENTRY_URL` selects the deployment. No per-call auth is needed.\n\n**Parameters:**\n- `organization`: organization slug. Omit to use `SENTRY_ORG`.\n- `issueId` (required): numeric issue id from `sentry_search_issues`, e.g. `5432109876`.\n- `eventId`: `latest` (default), `oldest`, or a 32-character hexadecimal event id such as `a1b2c3d4e5f60718a9b0c1d2e3f40516`.\n\n**Size warning:** event bodies are large — stack traces (`entries[?type=='exception']`), breadcrumbs (`entries[?type=='breadcrumbs']`), and `contexts` routinely run to hundreds of KB. The shared 10 MiB response cap applies (a larger body is refused with a size error), and output that exceeds the tool-output limit is spilled to a resumable artifact you can page through with `artifact_read`. **Always pass `jq`** to narrow the body, e.g.:\n- `entries[?type=='exception'].data.values[].{type: type, value: value, frames: stacktrace.frames[-5:].{file: filename, fn: function, line: lineNo}}`\n- `entries[?type=='breadcrumbs'].data.values[-20:]`\n- `{message: message, tags: tags, contexts: contexts}`\n\nEvent **attachments** (minidumps, screenshots, log files) are never fetched by this tool — it returns only the event JSON.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.sentry.io/api/events/retrieve-an-issue-event/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "issueId"
      ],
      "type": "object",
      "properties": {
        "organization": {
          "description": "Organization slug, e.g. \"acme-corp\". Omit to use `SENTRY_ORG`.",
          "type": [
            "string",
            "null"
          ]
        },
        "issueId": {
          "description": "Numeric issue id the event belongs to, e.g. \"5432109876\".",
          "type": "string"
        },
        "eventId": {
          "description": "Which event: \"latest\" (default), \"oldest\", or a 32-character\nhexadecimal event id, e.g. \"a1b2c3d4e5f60718a9b0c1d2e3f40516\".",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: event\nbodies are large — always narrow them. Examples:\n\"entries[?type=='exception'].data.values[].{type: type, value: value}\"\nor \"{message: message, tags: tags, contexts: contexts}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sentry_get_issue",
    "description": "Get one Sentry **issue** — its title, culprit, status, assignee, event/user counts, first/last seen, and 24h/30d stats. Use it to understand an issue's blast radius; use `sentry_get_event` for the stack trace. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/0/organizations/{organization}/issues/{issueId}/`.\n\nAuthenticates with a Sentry **auth token** (`SENTRY_TOKEN`, scopes `org:read` + `project:read` + `event:read`) sent as `Authorization: Bearer`; `SENTRY_URL` selects the deployment. No per-call auth is needed.\n\n**Parameters:**\n- `organization`: organization slug. Omit to use `SENTRY_ORG`.\n- `issueId` (required): the numeric issue id (the `id` field from `sentry_search_issues`, e.g. `5432109876`) — not the short id like `PROJ-1A2`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{title: title, culprit: culprit, status: status, count: count, userCount: userCount, firstSeen: firstSeen, lastSeen: lastSeen}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.sentry.io/api/events/retrieve-an-issue/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "issueId"
      ],
      "type": "object",
      "properties": {
        "organization": {
          "description": "Organization slug, e.g. \"acme-corp\". Omit to use `SENTRY_ORG`.",
          "type": [
            "string",
            "null"
          ]
        },
        "issueId": {
          "description": "Numeric issue id, e.g. \"5432109876\" (the `id` field from\n`sentry_search_issues`, not the short id like \"PROJ-1A2\").",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: use\nthis to keep only needed fields and reduce token costs. Example:\n\"{title: title, culprit: culprit, count: count, firstSeen: firstSeen}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sentry_list_projects",
    "description": "List the **projects** in a Sentry organization — the starting point for finding a numeric project id to pass to `sentry_search_issues` / `sentry_list_releases`. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/0/organizations/{organization}/projects/`. Each project carries `id` (numeric, what the other tools' `project` filter expects), `slug`, `name`, `platform`, and `dateCreated`.\n\nAuthenticates with a Sentry **auth token** (`SENTRY_TOKEN`, scopes `org:read` + `project:read`) sent as `Authorization: Bearer`; `SENTRY_URL` selects sentry.io, the EU region (`https://de.sentry.io`), or a self-hosted server. No per-call auth is needed.\n\n**Parameters:**\n- `organization`: organization slug. Omit to use `SENTRY_ORG`.\n- `cursor`: opaque pagination cursor from a previous call's `nextCursor`.\n\n**Output:** `{\"data\": [...projects], \"nextCursor\": \"<cursor>\" | null}`. Pagination is explicit — pass `nextCursor` back as `cursor` for the next page; results are not aggregated.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{id: id, slug: slug, platform: platform}` (applied to the page array before wrapping).\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.sentry.io/api/organizations/list-an-organizations-projects/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "organization": {
          "description": "Organization slug, e.g. \"acme-corp\". Omit to use `SENTRY_ORG`.",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor": {
          "description": "Opaque pagination cursor from a previous call's `nextCursor`, e.g.\n\"1500000000000:100:0\". Omit for the first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression applied to the page's `data` array before it is\nwrapped with `nextCursor`. IMPORTANT: use this to keep only needed\nfields and reduce token costs. Example: \"[*].{id: id, slug: slug, platform: platform}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sentry_list_releases",
    "description": "List the **releases** in a Sentry organization — version, creation date, deploy info, and new-issue counts, so \"did 1.4.2 introduce this?\" can be answered. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/0/organizations/{organization}/releases/`. Each release carries `version`, `shortVersion`, `dateCreated`, `dateReleased`, `newGroups` (issues first seen in it), `projects`, and `lastDeploy`.\n\nAuthenticates with a Sentry **auth token** (`SENTRY_TOKEN`, scopes `org:read` + `project:read`) sent as `Authorization: Bearer`; `SENTRY_URL` selects the deployment. No per-call auth is needed.\n\n**Parameters:**\n- `organization`: organization slug. Omit to use `SENTRY_ORG`.\n- `query`: substring match on the version, e.g. `1.4` or a commit SHA prefix.\n- `project`: numeric project id (from `sentry_list_projects`) to scope to one project.\n- `sort`: `date` (default), `sessions`, `users`, `crash_free_users`, `crash_free_sessions`.\n- `cursor`: opaque pagination cursor from a previous call's `nextCursor`.\n\n**Output:** `{\"data\": [...releases], \"nextCursor\": \"<cursor>\" | null}`. Pagination is explicit — pass `nextCursor` back as `cursor`; results are not aggregated.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{version: version, dateCreated: dateCreated, newGroups: newGroups}` (applied to the page array before wrapping).\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.sentry.io/api/releases/list-an-organizations-releases/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "organization": {
          "description": "Organization slug, e.g. \"acme-corp\". Omit to use `SENTRY_ORG`.",
          "type": [
            "string",
            "null"
          ]
        },
        "query": {
          "description": "Filter releases whose version contains this text, e.g. \"1.4\" or a\ncommit SHA prefix.",
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Numeric project id to restrict to, e.g. \"4507000000000001\". Omit for\nall projects.",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort order: \"date\" (default), \"sessions\", \"users\", \"crash_free_users\",\nor \"crash_free_sessions\".",
          "type": [
            "string",
            "null"
          ]
        },
        "cursor": {
          "description": "Opaque pagination cursor from a previous call's `nextCursor`. Omit for\nthe first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression applied to the page's `data` array before it is\nwrapped with `nextCursor`. IMPORTANT: use this to keep only needed\nfields and reduce token costs. Example:\n\"[*].{version: version, dateCreated: dateCreated, newGroups: newGroups}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sentry_search_issues",
    "description": "Search Sentry **issues** (grouped errors) across an organization with Sentry's search syntax — \"what is broken in production right now\". Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/0/organizations/{organization}/issues/`. Each issue carries its numeric `id` (pass to `sentry_get_issue` / `sentry_get_event`), `shortId`, `title`, `culprit`, `level`, `status`, `count`, `userCount`, `firstSeen`, `lastSeen`, and `project`.\n\nAuthenticates with a Sentry **auth token** (`SENTRY_TOKEN`, scopes `org:read` + `project:read` + `event:read`) sent as `Authorization: Bearer`; `SENTRY_URL` selects the deployment. No per-call auth is needed.\n\n**Parameters:**\n- `organization`: organization slug. Omit to use `SENTRY_ORG`.\n- `query`: Sentry search syntax, exactly as typed in the Issues page search bar. Default `is:unresolved`. Examples: `is:unresolved level:error`, `is:unresolved release:1.4.2`, `is:unresolved assigned:me`, `error.type:TypeError`.\n- `project`: numeric project id (from `sentry_list_projects`) to scope to one project. Omit for all.\n- `statsPeriod`: relative window such as `24h`, `7d`, `14d`.\n- `environment`: e.g. `production`.\n- `sort`: `date` (last seen, default), `new` (first seen), `freq` (event count), `priority`, `user` (affected users).\n- `limit`: page size 1-100 (default 25). Keep small.\n- `cursor`: opaque pagination cursor from a previous call's `nextCursor`.\n\n**Output:** `{\"data\": [...issues], \"nextCursor\": \"<cursor>\" | null}`. Pagination is explicit — pass `nextCursor` back as `cursor`; results are not aggregated.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[*].{id: id, title: title, count: count, lastSeen: lastSeen}` (applied to the page array before wrapping).\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.sentry.io/api/events/list-an-organizations-issues/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "organization": {
          "description": "Organization slug, e.g. \"acme-corp\". Omit to use `SENTRY_ORG`.",
          "type": [
            "string",
            "null"
          ]
        },
        "query": {
          "description": "Sentry issue search query (the same syntax as the Issues page search\nbar), e.g. \"is:unresolved level:error release:1.4.2\" or\n\"is:unresolved assigned:me\". Defaults to \"is:unresolved\".",
          "type": [
            "string",
            "null"
          ]
        },
        "project": {
          "description": "Numeric project id to restrict the search to, e.g. \"4507000000000001\"\n(from `sentry_list_projects`). Omit for all projects the token can see.",
          "type": [
            "string",
            "null"
          ]
        },
        "statsPeriod": {
          "description": "Relative time window, e.g. \"24h\", \"7d\", \"14d\". Omit for Sentry's\ndefault (the organization's retention window).",
          "type": [
            "string",
            "null"
          ]
        },
        "environment": {
          "description": "Environment name to filter on, e.g. \"production\".",
          "type": [
            "string",
            "null"
          ]
        },
        "sort": {
          "description": "Sort order: \"date\" (last seen, default), \"new\" (first seen), \"freq\"\n(event count), \"priority\", or \"user\" (affected users).",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1-100 (default 25). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "cursor": {
          "description": "Opaque pagination cursor from a previous call's `nextCursor`. Omit for\nthe first page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression applied to the page's `data` array before it is\nwrapped with `nextCursor`. IMPORTANT: use this to keep only needed\nfields and reduce token costs. Example:\n\"[*].{id: id, title: title, count: count, lastSeen: lastSeen}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_channel_history",
    "description": "Read the messages in one Slack channel. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nThe incident-investigation read: fetch what was said in `#incidents` (or any channel the token can see) in a time window. Each message includes `ts`, `user`, `text`, `thread_ts` (when it has replies — pass it to `slack_thread_replies`), and `reactions`. Find the channel id with `slack_list_channels`.\n\nAuthenticates with the configured Slack OAuth token (`SLACK_TOKEN`) sent as `Authorization: Bearer`. Calls `conversations.history` (read-only; `channels:history` / `groups:history`).\n\n**Parameters:**\n- `channelId` (required): the channel id, e.g. `C0123ABCDEF`\n- `oldest` / `latest`: Unix timestamps (seconds, decimals allowed) bounding the window\n- `inclusive`: `true` to include messages exactly at `oldest`/`latest`\n- `limit`: page size (Slack caps at 999; keep it small to reduce token costs)\n- `cursor`: `response_metadata.next_cursor` from the previous page\n\n**Typical use — messages with text and author only:**\n- `jq`: `messages[*].{ts: ts, user: user, text: text}`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/methods/conversations.history\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "channelId"
      ],
      "type": "object",
      "properties": {
        "channelId": {
          "description": "The channel id, e.g. `C0123ABCDEF`. Find it with `slack_list_channels`.",
          "type": "string"
        },
        "oldest": {
          "description": "Only messages after this Unix timestamp (seconds; decimals allowed).",
          "type": [
            "string",
            "null"
          ]
        },
        "latest": {
          "description": "Only messages before this Unix timestamp (seconds; decimals allowed).",
          "type": [
            "string",
            "null"
          ]
        },
        "inclusive": {
          "description": "Include messages exactly at `oldest` / `latest`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "description": "Page size (Slack caps at 999). Keep this small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "cursor": {
          "description": "`response_metadata.next_cursor` from the previous page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. Example:\n`messages[*].{ts: ts, user: user, text: text}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_channel_info",
    "description": "Get one Slack channel's metadata by id. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nReturns `name`, `topic`, `purpose`, `created`, `creator`, `is_private`, `is_archived`, and `num_members` for the channel. Find the id with `slack_list_channels`.\n\nAuthenticates with the configured Slack OAuth token (`SLACK_TOKEN`) sent as `Authorization: Bearer`. Calls `conversations.info` (read-only; `channels:read` / `groups:read`).\n\n**Parameters:**\n- `channelId` (required): the channel id, e.g. `C0123ABCDEF`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/methods/conversations.info\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "channelId"
      ],
      "type": "object",
      "properties": {
        "channelId": {
          "description": "The channel id, e.g. `C0123ABCDEF`. Find it with `slack_list_channels`.",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_delete",
    "description": "Send an HTTP DELETE to the Slack Web API. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a bot/user OAuth token (`SLACK_TOKEN`, e.g. `xoxb-…`) sent as a Bearer token; no per-call auth is needed.\n\n**Note:** Slack does not delete via the HTTP DELETE verb — deletion is a POST *method* instead. Prefer `slack_post`:\n- `/chat.delete` - delete a message. Body: `{\"channel\": \"C123\", \"ts\": \"...\"}`.\n- `/reactions.remove` - remove a reaction. Body: `{\"channel\": \"C123\", \"timestamp\": \"...\", \"name\": \"thumbsup\"}`.\n\nThis verb exists for completeness and takes no body. A request can fail with HTTP 200 and `{\"ok\": false, \"error\": \"<code>\"}`, which is reclassified as an error automatically.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/web\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_get",
    "description": "Read any Slack data via the Slack Web API (GET). Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a bot/user OAuth token (`SLACK_TOKEN`, e.g. `xoxb-…`) sent as a Bearer token; no per-call auth is needed.\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` to filter response fields. Unfiltered responses are expensive.\n- To explore a schema, fetch ONE item (or a single page) with NO jq, then add a jq filter on later calls.\n\n**Slack quirks you must know:**\n- Endpoints are *methods* (`/conversations.list`, `/users.info`), not REST resources.\n- A request can **fail with HTTP 200** and `{\"ok\": false, \"error\": \"<code>\"}` in the body. This tool reclassifies `ok: false` as an error automatically, so a successful result always has `ok: true`.\n- Most read methods take inputs as **query params** (`queryParams`), not a body.\n\n**Common GET methods:**\n- `/auth.test` - verify the token and see the authed user/team\n- `/conversations.list` - channels (`types=public_channel,private_channel`, `limit`, `cursor`)\n- `/conversations.history` - messages in a channel (`channel`, `limit`, `oldest`, `latest`)\n- `/conversations.replies` - a thread (`channel`, `ts`)\n- `/users.list` / `/users.info` - users (`user` for info)\n- `/users.conversations` - channels a user is in\n\n**Pagination:** Slack uses cursor pagination. Read `response_metadata.next_cursor` and pass it back as the `cursor` query param.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `channels[*].{id: id, name: name}`, `members[*].name`, `messages[*].{user: user, text: text, ts: ts}`\n\nAPI reference: https://api.slack.com/web\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_list_channels",
    "description": "List the Slack channels the token can see. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nUse this to discover a channel's `id` (e.g. `C0123ABCDEF`) for `slack_channel_history`, `slack_channel_info`, and `slack_thread_replies`. Each entry includes `id`, `name`, `is_private`, `is_archived`, `num_members`, and `topic`/`purpose`.\n\nAuthenticates with the configured Slack OAuth token (`SLACK_TOKEN`, a bot `xoxb-…` or user `xoxp-…` token) sent as `Authorization: Bearer`. Calls `conversations.list` (read-only; needs the `channels:read` and, for private channels, `groups:read` scopes).\n\n**Parameters:**\n- `types`: comma-separated channel kinds — `public_channel` (default), `private_channel`, `mpim`, `im`\n- `excludeArchived`: `true` to omit archived channels\n- `limit`: page size (Slack caps at 1000; keep it small)\n- `cursor`: `response_metadata.next_cursor` from the previous page\n\n**Typical use — find a channel by name:**\n- `jq`: `channels[?name=='incidents'].{id: id, name: name}`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/methods/conversations.list\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "types": {
          "description": "Comma-separated channel kinds: `public_channel` (default),\n`private_channel`, `mpim`, `im`.",
          "type": [
            "string",
            "null"
          ]
        },
        "excludeArchived": {
          "description": "Omit archived channels.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "description": "Page size (Slack caps at 1000). Keep this small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "cursor": {
          "description": "`response_metadata.next_cursor` from the previous page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. Example to find\na channel by name: `channels[?name=='incidents'].{id: id, name: name}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_patch",
    "description": "Send an HTTP PATCH to the Slack Web API. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a bot/user OAuth token (`SLACK_TOKEN`, e.g. `xoxb-…`) sent as a Bearer token; no per-call auth is needed.\n\n**Note:** The Slack Web API is almost entirely GET and POST — there is rarely a reason to use PATCH. To edit a message use `slack_post` with `/chat.update`. This verb exists for completeness; a request can still fail with HTTP 200 and `{\"ok\": false, \"error\": \"<code>\"}`, which is reclassified as an error automatically.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/web\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_post",
    "description": "Call a Slack Web API write method via HTTP POST. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a bot/user OAuth token (`SLACK_TOKEN`, e.g. `xoxb-…`) sent as a Bearer token; no per-call auth is needed.\n\n**Slack quirks you must know:**\n- Endpoints are *methods* (`/chat.postMessage`), not REST resources. Almost all Slack writes are POST regardless of semantics.\n- The request body is sent as JSON (`Content-Type: application/json`), which Slack accepts for Bearer-authenticated calls.\n- A request can **fail with HTTP 200** and `{\"ok\": false, \"error\": \"<code>\"}`. This tool reclassifies `ok: false` as an error automatically.\n- Writing requires the matching bot scope (e.g. `chat:write`) and the bot must be a member of the target channel.\n\n**Common POST methods:**\n- `/chat.postMessage` - send a message. Body: `{\"channel\": \"C123\", \"text\": \"hi\"}` (or `blocks`).\n- `/chat.update` - edit a message. Body: `{\"channel\": \"C123\", \"ts\": \"...\", \"text\": \"...\"}`.\n- `/chat.postEphemeral` - message visible only to one user. Body adds `{\"user\": \"U123\"}`.\n- `/conversations.create` - create a channel. Body: `{\"name\": \"my-channel\"}`.\n- `/conversations.invite` - add users. Body: `{\"channel\": \"C123\", \"users\": \"U1,U2\"}`.\n- `/reactions.add` - add an emoji reaction. Body: `{\"channel\": \"C123\", \"timestamp\": \"...\", \"name\": \"thumbsup\"}`.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/web\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_put",
    "description": "Send an HTTP PUT to the Slack Web API. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with a bot/user OAuth token (`SLACK_TOKEN`, e.g. `xoxb-…`) sent as a Bearer token; no per-call auth is needed.\n\n**Note:** The Slack Web API is almost entirely GET and POST — there is rarely a reason to use PUT. Prefer `slack_post` (e.g. `/chat.update` to edit a message). This verb exists for completeness; a request can still fail with HTTP 200 and `{\"ok\": false, \"error\": \"<code>\"}`, which is reclassified as an error automatically.\n\n**Body:** pass the JSON request body as `body`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/web\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_search_messages",
    "description": "Search Slack messages across the workspace. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nFull-text search with Slack's query modifiers (`in:#incidents`, `from:@alice`, `after:2026-09-01`, quoted phrases). Each match includes `channel.name`, `user`, `username`, `ts`, `text`, and a `permalink`.\n\nAuthenticates with the configured Slack OAuth token (`SLACK_TOKEN`) sent as `Authorization: Bearer`. Calls `search.messages`, which Slack allows only for **user** tokens (`xoxp-…`, `search:read`); a bot token gets `not_allowed_token_type`.\n\n**Parameters:**\n- `query` (required): the search expression\n- `count`: results per page (max 100)\n- `page`: page number\n- `sort`: `score` (default) or `timestamp`\n- `sortDir`: `asc` or `desc`\n\n**Typical use — just the hits:**\n- `jq`: `messages.matches[*].{channel: channel.name, ts: ts, text: text}`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/methods/search.messages\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "query"
      ],
      "type": "object",
      "properties": {
        "query": {
          "description": "The search expression, with Slack modifiers (`in:#incidents`,\n`from:@alice`, `after:2026-09-01`, quoted phrases).",
          "type": "string"
        },
        "count": {
          "description": "Results per page (max 100).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "page": {
          "description": "Page number.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "sort": {
          "description": "`score` (default) or `timestamp`.",
          "type": [
            "string",
            "null"
          ]
        },
        "sortDir": {
          "description": "`asc` or `desc`.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. Example:\n`messages.matches[*].{channel: channel.name, ts: ts, text: text}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "slack_thread_replies",
    "description": "Read the replies in one Slack thread. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nGiven a channel id and the parent message's `ts` (the `thread_ts` on a message from `slack_channel_history`), returns the parent and its replies in order.\n\nAuthenticates with the configured Slack OAuth token (`SLACK_TOKEN`) sent as `Authorization: Bearer`. Calls `conversations.replies` (read-only; `channels:history` / `groups:history`).\n\n**Parameters:**\n- `channelId` (required): the channel id, e.g. `C0123ABCDEF`\n- `threadTs` (required): the parent message timestamp, e.g. `1700000000.123456`\n- `oldest` / `latest` / `inclusive` / `limit` / `cursor`: as for `slack_channel_history`\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://api.slack.com/methods/conversations.replies\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "channelId",
        "threadTs"
      ],
      "type": "object",
      "properties": {
        "channelId": {
          "description": "The channel id, e.g. `C0123ABCDEF`.",
          "type": "string"
        },
        "threadTs": {
          "description": "The parent message's `ts` (the `thread_ts` on a reply), e.g.\n`1700000000.123456`.",
          "type": "string"
        },
        "oldest": {
          "description": "Only replies after this Unix timestamp.",
          "type": [
            "string",
            "null"
          ]
        },
        "latest": {
          "description": "Only replies before this Unix timestamp.",
          "type": [
            "string",
            "null"
          ]
        },
        "inclusive": {
          "description": "Include replies exactly at `oldest` / `latest`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "limit": {
          "description": "Page size (Slack caps at 999).",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "cursor": {
          "description": "`response_metadata.next_cursor` from the previous page.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "snyk_get_project",
    "description": "Get one Snyk **project** by id — its name, type, origin, status, target file / branch, settings, and tags. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /rest/orgs/{orgId}/projects/{projectId}`. Project UUIDs come from `snyk_list_projects` (`data[].id`) or from an issue's `relationships.scan_item.data.id`.\n\nAuthenticates with a Snyk **API token** (`SNYK_TOKEN`) sent as `Authorization: token`. No per-call auth is needed.\n\n**Parameters:**\n- `orgId`: organization UUID. Omit to use the server's `SNYK_ORG_ID`; an error is returned when neither is set.\n- `projectId` (required): project UUID, e.g. `331ede0a-de94-456f-b788-166caeca58bf`.\n- `expand`: pass `target` to include the related target (repository / image, with its `display_name` and `url`) inline under `included[]`.\n\n**Response shape (JSON:API):** `data` carries `id`, `type: \"project\"`, `attributes` (`name`, `type`, `origin`, `status`, `target_file`, `target_reference`, `created`, `settings`, `tags`, `business_criticality`, `environment`, `lifecycle`) and `relationships` (`target`, `organization`, `importer`, `owner`).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `data.attributes.{name: name, type: type, status: status, branch: target_reference}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.snyk.io/snyk-api/reference/projects\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "projectId"
      ],
      "type": "object",
      "properties": {
        "orgId": {
          "description": "Organization UUID (Snyk UI: Org Settings → General → Organization ID).\nOmit to use `SNYK_ORG_ID` from the server configuration.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectId": {
          "description": "Project UUID, e.g. \"331ede0a-de94-456f-b788-166caeca58bf\" (the `id` of\na `snyk_list_projects` entry).",
          "type": "string"
        },
        "expand": {
          "description": "Include the related target (repository / image) inline: pass \"target\".\nOmit for the project alone.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data.attributes.{name: name, type: type, status: status, tags: tags}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "snyk_list_issues",
    "description": "List Snyk **issues** (vulnerabilities, license problems, code / IaC / cloud findings) in an organization — the findings Snyk has already recorded, without running a scan. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /rest/orgs/{orgId}/issues`. Scope it to one project with `scanItemId` + `scanItemType: \"project\"` (project UUIDs come from `snyk_list_projects`) to answer \"what is failing this repo\"; omit the scope to sweep the whole organization.\n\nAuthenticates with a Snyk **API token** (`SNYK_TOKEN`) sent as `Authorization: token`. No per-call auth is needed.\n\n**Parameters:**\n- `orgId`: organization UUID. Omit to use the server's `SNYK_ORG_ID`; an error is returned when neither is set.\n- `scanItemId` + `scanItemType` (`project` or `environment`): restrict to one scan item. Pass both or neither.\n- `effectiveSeverityLevel`: comma-separated `low`, `medium`, `high`, `critical` (e.g. `high,critical`).\n- `status`: `open` or `resolved`.\n- `type`: comma-separated issue types, e.g. `package_vulnerability,license,code,config,cloud`.\n- `ignored`: `true` for ignored issues only, `false` to exclude them.\n- `updatedAfter` / `createdAfter`: RFC 3339 timestamps, e.g. `2026-08-01T00:00:00Z`.\n- `limit`: page size 1-100 (default 20).\n- `startingAfter`: cursor for the next page (see below).\n\n**Response shape (JSON:API):** `data[]` entries carry `id`, `type: \"issue\"`, and `attributes` with `title`, `type`, `status`, `ignored`, `effective_severity_level`, `key`, `created_at` / `updated_at`, `problems[]` (the CVE / CWE / Snyk vuln ids with `source`, `id`, `url`), `coordinates[]` (where it is: `representations[].dependency.package_name` / `package_version`, `resource_path`, and `remedies[]`), and `resolution` (`type`, `resolved_at`) when resolved. `relationships.scan_item.data.id` names the project. Paging continues via `links.next`, a relative URL; extract its `starting_after` query value and pass it as `startingAfter` (pasting the whole `links.next` value is also accepted).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `data[*].{title: attributes.title, severity: attributes.effective_severity_level, status: attributes.status, pkg: attributes.coordinates[0].representations[0].dependency.package_name, ver: attributes.coordinates[0].representations[0].dependency.package_version}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.snyk.io/snyk-api/reference/issues\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "orgId": {
          "description": "Organization UUID (Snyk UI: Org Settings → General → Organization ID).\nOmit to use `SNYK_ORG_ID` from the server configuration.",
          "type": [
            "string",
            "null"
          ]
        },
        "scanItemId": {
          "description": "Restrict to issues on one scan item — a project UUID (from\n`snyk_list_projects`) or an environment UUID. Requires `scanItemType`.",
          "type": [
            "string",
            "null"
          ]
        },
        "scanItemType": {
          "description": "Kind of `scanItemId`: \"project\" or \"environment\". Required together\nwith `scanItemId`.",
          "type": [
            "string",
            "null"
          ]
        },
        "effectiveSeverityLevel": {
          "description": "Comma-separated effective severities to include: \"low\", \"medium\",\n\"high\", \"critical\" (e.g. \"high,critical\"). Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "description": "Issue status: \"open\" or \"resolved\". Omit for both.",
          "type": [
            "string",
            "null"
          ]
        },
        "type": {
          "description": "Comma-separated issue types, e.g. \"package_vulnerability,license,\ncode,config,cloud\". Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "ignored": {
          "description": "`true` for ignored issues only, `false` to exclude ignored issues.\nOmit for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "updatedAfter": {
          "description": "Only issues updated after this RFC 3339 timestamp, e.g.\n\"2026-08-01T00:00:00Z\".",
          "type": [
            "string",
            "null"
          ]
        },
        "createdAfter": {
          "description": "Only issues created after this RFC 3339 timestamp, e.g.\n\"2026-08-01T00:00:00Z\".",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1-100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "startingAfter": {
          "description": "Pagination cursor for the next page: the `starting_after` value from\nthe previous response's `links.next`. Pasting the whole `links.next`\nURL is accepted; the cursor is extracted from it.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data[*].{title: attributes.title, severity: attributes.effective_severity_level, pkg: attributes.coordinates[0].representations[0].dependency.package_name}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "snyk_list_orgs",
    "description": "List the Snyk **organizations** the configured API token can access. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /rest/orgs`. Start here: every other `snyk_*` tool is scoped to an organization UUID (`data[].id`), so use this to discover the `orgId` to pass (or set `SNYK_ORG_ID` once in the server configuration).\n\nAuthenticates with a Snyk **API token** (`SNYK_TOKEN`) sent as `Authorization: token`; `SNYK_API_BASE` selects the regional host and `SNYK_API_VERSION` the pinned REST `version`. No per-call auth is needed.\n\n**Parameters:**\n- `groupId`: restrict to one Snyk group (group UUID).\n- `slug`: restrict to the organization with this URL slug, e.g. `my-company`.\n- `limit`: page size 1-100 (default 20).\n- `startingAfter`: cursor for the next page (see below).\n\n**Response shape (JSON:API):** `data[]` entries carry `id` (the org UUID), `type: \"org\"`, and `attributes` (`name`, `slug`, `group_id`, `is_personal`). Paging continues via `links.next`, a relative URL; extract its `starting_after` query value and pass it as `startingAfter` (pasting the whole `links.next` value is also accepted).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `data[*].{id: id, name: attributes.name, slug: attributes.slug}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.snyk.io/snyk-api/reference/orgs\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "groupId": {
          "description": "Restrict to organizations in one Snyk group (group UUID, e.g.\n\"0d3728ec-eebf-484d-9c8d-3b6d3b1b7c9a\"). Omit for every organization\nthe token can see.",
          "type": [
            "string",
            "null"
          ]
        },
        "slug": {
          "description": "Restrict to the organization with this slug (the URL-safe name shown\nin the Snyk UI, e.g. \"my-company\"). Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1-100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "startingAfter": {
          "description": "Pagination cursor for the next page: the `starting_after` value from\nthe previous response's `links.next`. Pasting the whole `links.next`\nURL is accepted; the cursor is extracted from it.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data[*].{id: id, name: attributes.name, slug: attributes.slug}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "snyk_list_projects",
    "description": "List the Snyk **projects** in an organization — each monitored manifest (`package.json`, `pom.xml`), container image, or IaC file. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /rest/orgs/{orgId}/projects`. Use it to find the project UUID (`data[].id`) to pass to `snyk_get_project` or as `scanItemId` to `snyk_list_issues`.\n\nAuthenticates with a Snyk **API token** (`SNYK_TOKEN`) sent as `Authorization: token`. No per-call auth is needed.\n\n**Parameters:**\n- `orgId`: organization UUID (Snyk UI: Org Settings → General → Organization ID). Omit to use the server's `SNYK_ORG_ID`; an error is returned when neither is set.\n- `names`: comma-separated exact project names, e.g. `my-org/api:package.json`.\n- `targetId`: restrict to one target (the imported repository / image) by UUID.\n- `types`: comma-separated project types, e.g. `npm,maven,dockerfile,terraformconfig`.\n- `origins`: comma-separated integration origins, e.g. `github,gitlab,cli,ecr`.\n- `limit`: page size 1-100 (default 20).\n- `startingAfter`: cursor for the next page (see below).\n\n**Response shape (JSON:API):** `data[]` entries carry `id` (project UUID), `type: \"project\"`, `attributes` (`name`, `type`, `origin`, `status`, `target_file`, `target_reference` (branch), `created`, `settings`, `tags`), and `relationships.target.data.id`. Paging continues via `links.next`, a relative URL; extract its `starting_after` query value and pass it as `startingAfter` (pasting the whole `links.next` value is also accepted).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `data[*].{id: id, name: attributes.name, type: attributes.type, branch: attributes.target_reference}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://docs.snyk.io/snyk-api/reference/projects\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "orgId": {
          "description": "Organization UUID (Snyk UI: Org Settings → General → Organization ID).\nOmit to use `SNYK_ORG_ID` from the server configuration.",
          "type": [
            "string",
            "null"
          ]
        },
        "names": {
          "description": "Comma-separated project names to match exactly, e.g.\n\"my-org/api:package.json,my-org/web:package-lock.json\". Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "targetId": {
          "description": "Restrict to projects belonging to one target (target UUID — a target is\nthe imported repository / container image / cloud account).",
          "type": [
            "string",
            "null"
          ]
        },
        "types": {
          "description": "Comma-separated project types, e.g. \"npm,maven,dockerfile,terraformconfig\".\nOmit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "origins": {
          "description": "Comma-separated integration origins, e.g. \"github,gitlab,cli,ecr\".\nOmit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1-100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "startingAfter": {
          "description": "Pagination cursor for the next page: the `starting_after` value from\nthe previous response's `links.next`. Pasting the whole `links.next`\nURL is accepted; the cursor is extracted from it.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"data[*].{id: id, name: attributes.name, type: attributes.type, origin: attributes.origin}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sonarqube_get",
    "description": "Generic `GET` against the SonarQube/SonarCloud Web API for endpoints without a dedicated tool. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nUse the dedicated tools first — `sonarqube_quality_gate` (why a build failed) and `sonarqube_search_issues` (the offending lines). Reach for this for the long tail:\n- `/api/projects/search` — discover project keys.\n- `/api/measures/component` — coverage, duplication, ncloc, etc. (`metricKeys=coverage,duplicated_lines_density`).\n- `/api/hotspots/search` — security hotspots.\n- `/api/ce/task` — raw compute-engine task status by `id`.\n\nAuthenticates with a Sonar **user token** (`SONARQUBE_TOKEN`) sent as `Authorization: Bearer`; `SONARQUBE_URL` sets the base. No per-call auth is needed.\n\n**Parameters:**\n- `path`: the API endpoint path starting with `/`, e.g. `/api/measures/component`.\n- `queryParams`: key-value pairs, e.g. `{\"component\": \"my-project\", \"metricKeys\": \"coverage,bugs\"}`.\n- `jq`: JMESPath to keep only the fields you need (always use this to cut token costs).\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://next.sonarqube.com/sonarqube/web_api\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sonarqube_quality_gate",
    "description": "Report a SonarQube/SonarCloud **quality gate** result — the failing conditions that explain *why a CI build failed Sonar*. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/qualitygates/project_status`. The response's `projectStatus.status` is `OK`/`ERROR`, and `projectStatus.conditions[]` lists each condition with its `metricKey`, `comparator`, `errorThreshold`, and the `actualValue` — e.g. `new_coverage` `LT` `80` with `actualValue: 63.4`. That is the literal failure reason.\n\nAuthenticates with a Sonar **user token** (`SONARQUBE_TOKEN`) sent as `Authorization: Bearer`; `SONARQUBE_URL` sets the base (`https://sonar.mycorp.com` or `https://sonarcloud.io`). No per-call auth is needed.\n\n**Identify the analysis with exactly one of:**\n- `analysisId` — used as-is.\n- `ceTaskId` — the scanner compute-engine task id printed in the CI log's `report-task.txt` (grab it from `circleci_logs`). This tool resolves it to an `analysisId` via `/api/ce/task` for you, so you can go straight from a CircleCI log to the gate result.\n- `projectKey` — with optional `branch` **or** `pullRequest` (mutually exclusive). Alone, it reports the main branch's latest analysis.\n\n**Typical flow after a red build:** `circleci_logs` (find the failed Sonar step and its `ceTaskId`) → `sonarqube_quality_gate` (failing conditions) → `sonarqube_search_issues` (the exact offending lines).\n\n**SonarCloud:** pass `organization`. Omit it for self-hosted SonarQube.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `projectStatus.conditions[?status=='ERROR']`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://next.sonarqube.com/sonarqube/web_api/api/qualitygates/project_status\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "projectKey": {
          "description": "Project key (as shown in Sonar). Combine with `branch` or `pullRequest`\nto target a specific analysis; alone it reports the main branch's latest.",
          "type": [
            "string",
            "null"
          ]
        },
        "branch": {
          "description": "Branch name whose latest analysis to report. Mutually exclusive with\n`pullRequest`. Requires `projectKey`.",
          "type": [
            "string",
            "null"
          ]
        },
        "pullRequest": {
          "description": "Pull-request id whose analysis to report. Mutually exclusive with\n`branch`. Requires `projectKey`.",
          "type": [
            "string",
            "null"
          ]
        },
        "analysisId": {
          "description": "Analysis id (from `/api/project_analyses/search` or a prior `ceTaskId`\nresolution). Used verbatim; takes precedence over the other selectors.",
          "type": [
            "string",
            "null"
          ]
        },
        "ceTaskId": {
          "description": "Scanner compute-engine task id — the `ceTaskId` printed in the CI log's\n`report-task.txt`. Resolved to its `analysisId` via `/api/ce/task` first,\nso you can go straight from a CircleCI log to the gate result.",
          "type": [
            "string",
            "null"
          ]
        },
        "organization": {
          "description": "SonarCloud organization key. Required on SonarCloud; omit for\nself-hosted SonarQube.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"projectStatus.conditions[?status=='ERROR']\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "sonarqube_search_issues",
    "description": "Search SonarQube/SonarCloud **issues** (bugs, vulnerabilities, code smells) for a project — the specific lines behind a quality-gate failure. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /api/issues/search`. Each returned issue carries its `rule`, `severity`, `type`, `message`, `component` (file), and `line`, so \"2 new bugs\" becomes \"unclosed resource at `Foo.java:41`\".\n\nAuthenticates with a Sonar **user token** (`SONARQUBE_TOKEN`) sent as `Authorization: Bearer`; `SONARQUBE_URL` sets the base. No per-call auth is needed.\n\n**Parameters:**\n- `componentKeys` (required): usually the project key, e.g. `my-org_my-repo`. Comma-separate for multiple.\n- `branch` **or** `pullRequest` (mutually exclusive): scope to that analysis. For a red PR build, pass the `pullRequest` id to get exactly the issues that failed it.\n- `types`: `BUG`, `VULNERABILITY`, `CODE_SMELL` (comma-separated).\n- `severities`: `INFO`, `MINOR`, `MAJOR`, `CRITICAL`, `BLOCKER`.\n- `statuses`: e.g. `OPEN,CONFIRMED,REOPENED`.\n- `resolved`: `false` for open issues only.\n- `pageSize`: Sonar's `ps` (max 500) — keep small.\n\n**SonarCloud:** pass `organization`. Omit it for self-hosted SonarQube.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `issues[*].{rule: rule, msg: message, file: component, line: line}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://next.sonarqube.com/sonarqube/web_api/api/issues/search\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "componentKeys"
      ],
      "type": "object",
      "properties": {
        "componentKeys": {
          "description": "Component keys to search, comma-separated. Usually a single project key,\ne.g. \"my-org_my-repo\".",
          "type": "string"
        },
        "branch": {
          "description": "Restrict to a branch's issues. Mutually exclusive with `pullRequest`.",
          "type": [
            "string",
            "null"
          ]
        },
        "pullRequest": {
          "description": "Restrict to a pull request's issues. Mutually exclusive with `branch`.",
          "type": [
            "string",
            "null"
          ]
        },
        "types": {
          "description": "Comma-separated issue types to include: `BUG`, `VULNERABILITY`,\n`CODE_SMELL`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "severities": {
          "description": "Comma-separated severities: `INFO`, `MINOR`, `MAJOR`, `CRITICAL`,\n`BLOCKER`. Omit for all.",
          "type": [
            "string",
            "null"
          ]
        },
        "statuses": {
          "description": "Comma-separated issue statuses, e.g. `OPEN,CONFIRMED,REOPENED`. Omit for\nall.",
          "type": [
            "string",
            "null"
          ]
        },
        "resolved": {
          "description": "Filter by resolution state: `false` for unresolved (open) issues only,\n`true` for resolved ones. Omit for both.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pageSize": {
          "description": "Page size (Sonar's `ps`, max 500). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "organization": {
          "description": "SonarCloud organization key. Required on SonarCloud; omit for\nself-hosted SonarQube.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"issues[*].{rule: rule, msg: message, file: component, line: line}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "splunk_create_job",
    "description": "Start an asynchronous Splunk SPL search and return its search ID (`sid`).\n\nUse this for searches that may take longer or produce results that should be paged. Pass the returned `sid` to `splunk_job_results`. Include explicit `earliestTime` and `latestTime` bounds for indexed-data searches.\n\nExample: `{\"search\":\"search index=main sourcetype=access_combined | stats count by status\",\"earliestTime\":\"-24h@h\",\"latestTime\":\"now\",\"jq\":\"sid\"}`\n\nRequires `SPLUNK_URL` and `SPLUNK_TOKEN`.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "search"
      ],
      "type": "object",
      "properties": {
        "search": {
          "description": "SPL query to run asynchronously.",
          "type": "string"
        },
        "earliestTime": {
          "description": "Inclusive earliest time. REST searches otherwise default to all time,\nso callers should normally provide this bound.",
          "type": [
            "string",
            "null"
          ]
        },
        "latestTime": {
          "description": "Inclusive latest time.",
          "type": [
            "string",
            "null"
          ]
        },
        "maxCount": {
          "description": "Maximum number of stored results, up to Splunk's configured limits.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "maxTime": {
          "description": "Maximum seconds Splunk may run the search before finalizing it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "splunk_job_results",
    "description": "Fetch a page of results for a Splunk search job.\n\nPass the `sid` returned by `splunk_create_job`. Use `count` and `offset` to page large result sets. If the search has not completed, Splunk may return no final results yet.\n\nThe result body is streamed and normalized to an atomic canonical-NDJSON artifact with a hard byte limit, bounded head/tail preview, SHA-256 checksum, and manifest sidecar. `jq` is not applied to this streamed response.\n\nExample: `{\"sid\":\"1712345678.42\",\"count\":100,\"offset\":0,\"jq\":\"rows\"}`\n\nRequires `SPLUNK_URL` and `SPLUNK_TOKEN`.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "sid"
      ],
      "type": "object",
      "properties": {
        "sid": {
          "description": "Search ID returned by `splunk_create_job`.",
          "type": "string"
        },
        "count": {
          "description": "Number of results to return. Defaults to Splunk's endpoint default.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "offset": {
          "description": "Zero-based result offset for pagination.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "splunk_list_saved_searches",
    "description": "List reports, alerts, and other saved searches visible to the authenticated Splunk user.\n\nUse `search` to apply a Splunk collection filter and `count`/`offset` for pagination. Filter the response with JMESPath to reduce token usage.\n\nExample: `{\"count\":50,\"jq\":\"entry[*].{name:name,search:content.search,disabled:content.disabled}\"}`\n\nRequires `SPLUNK_URL` and `SPLUNK_TOKEN`.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "search": {
          "description": "Optional Splunk collection filter, for example `name=\"Errors by host\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "count": {
          "description": "Number of saved searches to return.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "offset": {
          "description": "Zero-based offset for pagination.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "splunk_search",
    "description": "Run a bounded SPL search and return results as they become available.\n\nUses Splunk's versioned `/services/search/v2/jobs/export` endpoint with `json_rows` output. Include `earliestTime` and `latestTime` for indexed-data searches; Splunk REST searches otherwise default to all time. Keep result sets small with SPL commands such as `head`, `fields`, `table`, or aggregation commands.\n\nThe export body is consumed incrementally and normalized to an atomic canonical-NDJSON artifact with a hard stream-level byte limit, bounded head/tail preview, SHA-256 checksum, and manifest sidecar. This protects chunked responses without `Content-Length`. `jq` is not applied to streamed exports; filter the downloaded artifact instead.\n\nExample: `{\"search\":\"search index=main error | stats count by host | head 20\",\"earliestTime\":\"-15m\",\"latestTime\":\"now\",\"jq\":\"rows\"}`\n\nRequires `SPLUNK_URL` and `SPLUNK_TOKEN`.\n\n`timePartitions` (2-16) is an opt-in preview for plain event searches only (`search ...` with no pipeline or embedded time modifier). Splunk documents the event-time window as `earliest <= _time < latest`; exact RFC3339 or epoch-second bounds are therefore translated directly into adjacent half-open ranges using nine-digit decimal epoch seconds. Transforming/pipelined searches, relative, missing, invalid, reversed, or precision-ambiguous bounds retain the unchanged single-request path because partitioning could change SPL semantics. Until bounded merge/deduplication lands, the returned artifact is a conservative `partial` partition-set status naming the atomic, checksummed canonical parts, not a final combined artifact.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "search"
      ],
      "type": "object",
      "properties": {
        "search": {
          "description": "Splunk Search Processing Language (SPL) query. Searching indexed data\nnormally starts with `search`, for example\n`search index=main error | head 20`.",
          "type": "string"
        },
        "earliestTime": {
          "description": "Inclusive earliest time, such as `-15m`, `-24h@h`, or an ISO timestamp.",
          "type": [
            "string",
            "null"
          ]
        },
        "latestTime": {
          "description": "Inclusive latest time, such as `now` or an ISO timestamp.",
          "type": [
            "string",
            "null"
          ]
        },
        "timePartitions": {
          "description": "Split an absolute range into this many requests (2-16). Relative or\nimprecise bounds always retain the single-request path.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint8",
          "minimum": 0,
          "maximum": 255
        },
        "maxTime": {
          "description": "Maximum seconds Splunk may run the search before finalizing it.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the JSON rows response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "teamcity_delete",
    "description": "Delete TeamCity resources, for example path `builds/id:123`. This is destructive; confirm the intended resource before calling.\n\nGeneric `DELETE` against the TeamCity REST API. Paths may be relative (`projects`) or include `/app/rest/` (`/app/rest/projects`); the prefix is added when absent. Configure `TEAMCITY_URL` as the server URL, including any context path but excluding `/app/rest`, and `TEAMCITY_TOKEN` as a personal access token.\n\nReturns TOON by default; use `outputFormat: \"json\"` for JSON and `jq` for JMESPath filtering. Pass TeamCity `locator` and `fields` through `queryParams`. Pagination is explicit: follow `nextHref` using another call; results are not automatically aggregated.\n\nAPI reference: https://www.jetbrains.com/help/teamcity/rest/teamcity-rest-api-documentation.html\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "REST endpoint, e.g. `builds` or `/app/rest/builds/id:123`.",
          "type": "string"
        },
        "queryParams": {
          "description": "Query parameters, including `locator` for selection and `fields` for projection.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter or transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "teamcity_get",
    "description": "Read TeamCity builds, build configurations, projects, agents, and queued builds. For example, path `builds` with queryParams `{\"locator\":\"buildType:MyBuild,count:20\",\"fields\":\"build(id,number,status,webUrl),nextHref\"}`.\n\nGeneric `GET` against the TeamCity REST API. Paths may be relative (`projects`) or include `/app/rest/` (`/app/rest/projects`); the prefix is added when absent. Configure `TEAMCITY_URL` as the server URL, including any context path but excluding `/app/rest`, and `TEAMCITY_TOKEN` as a personal access token.\n\nReturns TOON by default; use `outputFormat: \"json\"` for JSON and `jq` for JMESPath filtering. Pass TeamCity `locator` and `fields` through `queryParams`. Pagination is explicit: follow `nextHref` using another call; results are not automatically aggregated.\n\nAPI reference: https://www.jetbrains.com/help/teamcity/rest/teamcity-rest-api-documentation.html\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "REST endpoint, e.g. `builds` or `/app/rest/builds/id:123`.",
          "type": "string"
        },
        "queryParams": {
          "description": "Query parameters, including `locator` for selection and `fields` for projection.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter or transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "teamcity_post",
    "description": "Create TeamCity resources or trigger/cancel builds. To queue a build, use path `buildQueue` and body `{\"buildType\":{\"id\":\"MyBuild\"}}`. This can start CI jobs.\n\nGeneric `POST` against the TeamCity REST API. Paths may be relative (`projects`) or include `/app/rest/` (`/app/rest/projects`); the prefix is added when absent. Configure `TEAMCITY_URL` as the server URL, including any context path but excluding `/app/rest`, and `TEAMCITY_TOKEN` as a personal access token.\n\nReturns TOON by default; use `outputFormat: \"json\"` for JSON and `jq` for JMESPath filtering. Pass TeamCity `locator` and `fields` through `queryParams`. Pagination is explicit: follow `nextHref` using another call; results are not automatically aggregated.\n\nAPI reference: https://www.jetbrains.com/help/teamcity/rest/teamcity-rest-api-documentation.html\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "REST endpoint, e.g. `builds` or `/app/rest/builds/id:123`.",
          "type": "string"
        },
        "body": {
          "description": "JSON request body for the selected endpoint."
        },
        "queryParams": {
          "description": "Query parameters, including `locator` for selection and `fields` for projection.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter or transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "teamcity_put",
    "description": "Update TeamCity resources using JSON bodies on endpoints that accept JSON. For example, path `buildQueue/pausedState` and body `{\"paused\":true,\"reason\":\"Maintenance\"}`. Endpoints requiring raw text or XML bodies are not supported by this tool.\n\nGeneric `PUT` against the TeamCity REST API. Paths may be relative (`projects`) or include `/app/rest/` (`/app/rest/projects`); the prefix is added when absent. Configure `TEAMCITY_URL` as the server URL, including any context path but excluding `/app/rest`, and `TEAMCITY_TOKEN` as a personal access token.\n\nReturns TOON by default; use `outputFormat: \"json\"` for JSON and `jq` for JMESPath filtering. Pass TeamCity `locator` and `fields` through `queryParams`. Pagination is explicit: follow `nextHref` using another call; results are not automatically aggregated.\n\nAPI reference: https://www.jetbrains.com/help/teamcity/rest/teamcity-rest-api-documentation.html\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "REST endpoint, e.g. `builds` or `/app/rest/builds/id:123`.",
          "type": "string"
        },
        "body": {
          "description": "JSON request body for the selected endpoint."
        },
        "queryParams": {
          "description": "Query parameters, including `locator` for selection and `fields` for projection.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter or transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "vercel_get_deployment",
    "description": "Get one Vercel **deployment** by id or hostname — its `readyState`, `url`, `target`, Git metadata, build configuration, and (for failed builds) `errorCode` / `errorMessage`. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v13/deployments/{deployment}`. For the log lines behind an `ERROR` state, follow up with `vercel_get_deployment_logs`.\n\nAuthenticates with a Vercel **account token** (`VERCEL_TOKEN`) sent as `Authorization: Bearer`. No per-call auth is needed.\n\n**Scope:** `teamId` (a `team_...` id from Team Settings → General) selects the team; it overrides `VERCEL_TEAM_ID`. When neither is set the request is scoped to the personal account — a team-owned deployment returns 404 without `teamId`.\n\n**Parameters:**\n- `deployment` (required): the deployment id (`dpl_...`) or its hostname, e.g. `storefront-abc123-acme.vercel.app` (no scheme, no path).\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `{id: id, state: readyState, url: url, target: target, error: errorMessage, commit: meta.githubCommitSha}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://vercel.com/docs/rest-api/reference/endpoints/deployments/get-a-deployment-by-id-or-url\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "deployment"
      ],
      "type": "object",
      "properties": {
        "deployment": {
          "description": "Deployment id (`dpl_xxx`) or deployment hostname such as\n\"storefront-abc123-acme.vercel.app\" (without scheme or path).",
          "type": "string"
        },
        "teamId": {
          "description": "Vercel team id (`team_xxx`, Team Settings → General) that owns the\ndeployment. Overrides `VERCEL_TEAM_ID`. Omit for the personal account\nwhen no default team is configured. Required for team-owned projects.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"{id: id, state: readyState, url: url, error: errorMessage}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "vercel_get_deployment_logs",
    "description": "Read the **build and runtime log** of a Vercel deployment — the `stdout` / `stderr` lines that explain why a build failed. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v3/deployments/{deployment}/events` and returns a JSON array of events. Each event carries `type` (`stdout`, `stderr`, `command`, `delimiter`, `exit`, …), `created` (ms timestamp), and `payload` (`text`, `serial`, and for runtime events `statusCode` / `path`).\n\n**Logs are bounded, not streamed.** Each call returns at most `limit` events (default 100, max 1000), and the shared response cap may spill a large page to a resumable artifact. Streaming (`follow=1`) is never requested; to see the end of a long build, call with `direction: \"backward\"` and a small `limit`, or narrow with `since` / `until`.\n\nAuthenticates with a Vercel **account token** (`VERCEL_TOKEN`) sent as `Authorization: Bearer`. No per-call auth is needed.\n\n**Scope:** `teamId` (a `team_...` id from Team Settings → General) selects the team; it overrides `VERCEL_TEAM_ID`. When neither is set the request is scoped to the personal account — a team-owned deployment returns 404 without `teamId`.\n\n**Parameters:**\n- `deployment` (required): the deployment id (`dpl_...`) or its hostname, e.g. `storefront-abc123-acme.vercel.app`.\n- `limit`: 1–1000 events (default 100).\n- `direction`: `forward` (oldest first, default) or `backward` (newest first).\n- `builds`: include build logs (default `true`).\n- `since` / `until`: millisecond timestamps as strings bounding the events.\n- `statusCode`: filter runtime request events by status, e.g. `500` or `5xx`.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `[?type=='stderr'].{at: created, text: payload.text}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://vercel.com/docs/rest-api/reference/endpoints/deployments/get-deployment-events\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "deployment"
      ],
      "type": "object",
      "properties": {
        "deployment": {
          "description": "Deployment id (`dpl_xxx`) or deployment hostname such as\n\"storefront-abc123-acme.vercel.app\" (without scheme or path).",
          "type": "string"
        },
        "teamId": {
          "description": "Vercel team id (`team_xxx`, Team Settings → General) that owns the\ndeployment. Overrides `VERCEL_TEAM_ID`. Omit for the personal account\nwhen no default team is configured. Required for team-owned projects.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Maximum number of log events to return, 1–1000 (default 100). Output is\nalso bounded by the shared response cap; keep this small.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "direction": {
          "description": "Read order: `forward` (oldest first, default) or `backward` (newest\nfirst — use with a small `limit` to see the tail of a failed build).",
          "type": [
            "string",
            "null"
          ]
        },
        "builds": {
          "description": "Include build (compile) logs, not just runtime events. Default true.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "since": {
          "description": "Only events at or after this millisecond timestamp, e.g.\n\"1718000000000\".",
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "description": "Only events at or before this millisecond timestamp, e.g.\n\"1718000000000\".",
          "type": [
            "string",
            "null"
          ]
        },
        "statusCode": {
          "description": "Filter runtime request events by HTTP status code, e.g. \"500\" or the\nrange form \"5xx\".",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"[?type=='stderr'].{at: created, text: payload.text}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "vercel_list_deployments",
    "description": "List Vercel **deployments** — the deploy history of a project (or of the whole team/account), so a failed or stale deployment can be found by state, target, or time. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v6/deployments`. Each deployment carries its `uid` (`dpl_...`), `url`, `state` (`BUILDING`, `ERROR`, `INITIALIZING`, `QUEUED`, `READY`, `CANCELED`), `target`, `created`, and Git `meta` (commit, branch, author). Use `vercel_get_deployment` / `vercel_get_deployment_logs` with the `uid` to drill in.\n\nAuthenticates with a Vercel **account token** (`VERCEL_TOKEN`) sent as `Authorization: Bearer`. No per-call auth is needed.\n\n**Scope:** `teamId` (a `team_...` id from Team Settings → General) selects the team; it overrides `VERCEL_TEAM_ID`. When neither is set the request is scoped to the personal account — pass `teamId` for team-owned projects.\n\n**Parameters:**\n- `projectId`: project id (`prj_...`) or project name. Omit for every project in scope.\n- `app`: application (project) name filter.\n- `state`: comma-separated deployment states, e.g. `ERROR` or `ERROR,CANCELED`.\n- `target`: `production` or `preview`.\n- `limit`: page size 1–100 (default 20) — keep small.\n- `since` / `until`: millisecond timestamps as strings bounding creation time.\n\n**Pagination:** the response body carries `pagination.next` / `pagination.prev` (millisecond timestamps); pass `pagination.next` as `until` to page further back in history.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `deployments[*].{uid: uid, state: state, url: url, created: created, commit: meta.githubCommitMessage}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://vercel.com/docs/rest-api/reference/endpoints/deployments/list-deployments\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "teamId": {
          "description": "Vercel team id (`team_xxx`, Team Settings → General) that owns the\ndeployments. Overrides `VERCEL_TEAM_ID`. Omit for the personal account\nwhen no default team is configured. Required for team-owned projects.",
          "type": [
            "string",
            "null"
          ]
        },
        "projectId": {
          "description": "Project id (`prj_xxx`) or project name to list deployments for, e.g.\n\"storefront\". Omit for every project in scope.",
          "type": [
            "string",
            "null"
          ]
        },
        "app": {
          "description": "Deployment application name filter (the `app` field, usually the\nproject name).",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "description": "Filter by deployment state, comma-separated: `BUILDING`, `ERROR`,\n`INITIALIZING`, `QUEUED`, `READY`, `CANCELED`. Example: \"ERROR\" to find\nfailed deployments.",
          "type": [
            "string",
            "null"
          ]
        },
        "target": {
          "description": "Filter by target environment: `production` or `preview`.",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "since": {
          "description": "Only deployments created after this millisecond timestamp, e.g.\n\"1718000000000\".",
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "description": "Only deployments created before this millisecond timestamp, e.g.\n\"1718000000000\". Use the previous page's `pagination.next` to page\nbackwards through history.",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"deployments[*].{uid: uid, state: state, url: url, created: created}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "vercel_list_projects",
    "description": "List Vercel **projects** in scope — the first step to find a project id or name before listing its deployments. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nCalls `GET /v9/projects`. Each project carries its `id` (`prj_...`), `name`, `framework`, linked Git repository (`link`), and `latestDeployments`.\n\nAuthenticates with a Vercel **account token** (`VERCEL_TOKEN`) sent as `Authorization: Bearer`. No per-call auth is needed.\n\n**Scope:** `teamId` (a `team_...` id from Team Settings → General) selects the team; it overrides `VERCEL_TEAM_ID`. When neither is set the request is scoped to the personal account, and team-owned projects are not visible — pass `teamId` for team-owned projects.\n\n**Parameters:**\n- `search`: name substring filter, e.g. `storefront`.\n- `repoUrl`: only projects linked to this Git repository URL.\n- `limit`: page size 1–100 (default 20) — keep small.\n- `from` / `until`: millisecond timestamps as strings bounding the creation time.\n\n**Pagination:** the response body carries `pagination.next`; pass it as `from` on the next call to fetch the following page. Results are not aggregated automatically.\n\n**IMPORTANT - Cost Optimization:** use `jq` to keep only what you need, e.g. `projects[*].{id: id, name: name, framework: framework}`.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\nAPI reference: https://vercel.com/docs/rest-api/reference/endpoints/projects/retrieve-a-list-of-projects\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "teamId": {
          "description": "Vercel team id (`team_xxx`, Team Settings → General) that owns the\nprojects. Overrides `VERCEL_TEAM_ID`. Omit for the personal account when\nno default team is configured. Required for team-owned projects.",
          "type": [
            "string",
            "null"
          ]
        },
        "search": {
          "description": "Filter projects by name substring, e.g. \"storefront\".",
          "type": [
            "string",
            "null"
          ]
        },
        "limit": {
          "description": "Page size, 1–100 (default 20). Keep small to reduce token costs.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "from": {
          "description": "Pagination cursor: pass the previous response's `pagination.next` value\n(a millisecond timestamp such as \"1718000000000\") to fetch the next page.",
          "type": [
            "string",
            "null"
          ]
        },
        "until": {
          "description": "Only projects created before this millisecond timestamp, e.g.\n\"1718000000000\".",
          "type": [
            "string",
            "null"
          ]
        },
        "repoUrl": {
          "description": "Filter to the project(s) linked to this Git repository URL, e.g.\n\"https://github.com/acme/storefront\".",
          "type": [
            "string",
            "null"
          ]
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT: always\nuse this to extract only needed fields and reduce token costs.\nExample: \"projects[*].{id: id, name: name, framework: framework}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "wrds_describe_table",
    "description": "Describe the columns of one WRDS table or view. Pass `library` (Postgres schema, e.g. `crsp`) and `table` (e.g. `dsf`) — find them with `wrds_list_libraries` and `wrds_list_tables`. Returns each column's `column_name`, `data_type`, and `is_nullable`, in column order. Use this to learn the exact column names and types before writing a `wrds_query`, so your `SELECT` and `WHERE` clauses reference real fields.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "library",
        "table"
      ],
      "type": "object",
      "properties": {
        "library": {
          "description": "WRDS library (Postgres schema) containing the table, e.g. \"crsp\".",
          "type": "string"
        },
        "table": {
          "description": "Table or view name within the library, e.g. \"dsf\".",
          "type": "string"
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.\nExample: \"[*].{col: column_name, type: data_type}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "wrds_list_libraries",
    "description": "List the WRDS libraries (PostgreSQL schemas) your WRDS account can access — e.g. `crsp`, `comp`, `ibes`, `ff`, `taqmsec`. A WRDS \"library\" is the top-level grouping of related datasets; pick one and pass it to `wrds_list_tables` to see the tables/views it contains. Access reflects your institution's WRDS subscriptions, so the list varies by account. Takes no required input.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "properties": {
        "jq": {
          "description": "JMESPath expression to filter/transform the response.\nExample: \"[*].library\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "type": "object"
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "wrds_list_tables",
    "description": "List the tables and views inside one WRDS library (PostgreSQL schema). Pass `library` (e.g. `crsp`, `comp`, `ff`) — discover valid library names with `wrds_list_libraries`. Returns each entry's `table_name` and `table_type` (`BASE TABLE` or `VIEW`; WRDS exposes most datasets as views). Use the returned name with `wrds_describe_table` to see its columns, then with `wrds_query` to read it (as `library.table_name`).\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "library"
      ],
      "type": "object",
      "properties": {
        "library": {
          "description": "WRDS library (Postgres schema) to list, e.g. \"crsp\", \"comp\", \"ff\".\nDiscover available libraries with `wrds_list_libraries`.",
          "type": "string"
        },
        "rowLimit": {
          "description": "Maximum rows to return. Defaults to 1000; capped at 100000.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.\nExample: \"[*].table_name\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "wrds_query",
    "description": "Run a read-only SQL query against WRDS (Wharton Research Data Services). WRDS is a PostgreSQL data platform for finance, accounting, and economics research (CRSP, Compustat, IBES, TAQ, etc.); there is no REST API, so this tool queries the database directly over an SSL connection.\n\nReference a dataset as `library.table`, where a WRDS \"library\" is a Postgres schema — e.g. `crsp.dsf` (CRSP daily stock file), `comp.funda` (Compustat fundamentals annual), `ff.factors_daily` (Fama-French factors). Use `wrds_list_libraries`, `wrds_list_tables`, and `wrds_describe_table` to discover what is available and the exact column names before querying.\n\nThe query must be a single read-only `SELECT` (the session is forced read-only; writes/DDL are rejected). WRDS tables are very large, so ALWAYS constrain with a `WHERE` clause and keep `rowLimit` small. Use `jq` (JMESPath) to project only the columns you need and cut token cost.\n\nExample: `SELECT permno, date, ret, prc, vol FROM crsp.dsf WHERE permno = 14593 AND date >= '2023-01-01' ORDER BY date`.\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "sql"
      ],
      "type": "object",
      "properties": {
        "sql": {
          "description": "Read-only SQL SELECT to run against WRDS. Reference tables as\n`library.table` (a WRDS library is a Postgres schema), e.g.\n`SELECT permno, date, ret FROM crsp.dsf WHERE date >= '2023-01-01' LIMIT 100`.\nDiscover libraries/tables/columns with `wrds_list_libraries`,\n`wrds_list_tables`, and `wrds_describe_table`.",
          "type": "string"
        },
        "rowLimit": {
          "description": "Maximum rows to return (token-cost guard). Defaults to 1000; capped at\n100000. Always prefer a small limit and a tight `WHERE` clause — WRDS\ntables can be enormous.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the rows. IMPORTANT: always use\nthis to extract only needed fields and reduce token costs.\nExample: \"[*].{permno: permno, ret: ret}\".",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "zoom_delete",
    "description": "Delete Zoom resources via DELETE to the Zoom REST API v2.\n\nAuthenticates with Server-to-Server OAuth (auto-renewed bearer).\n\n**Cancel/delete a meeting:** `DELETE /meetings/{meetingId}`\n- Optional query params: `occurrence_id` (delete a single occurrence of a recurring meeting), `schedule_for_reminder=true` (email the host/alternative hosts), `cancel_meeting_reminder=true` (notify registrants).\n\n**Other common deletes:**\n- `DELETE /meetings/{meetingId}/registrants/{registrantId}` - remove a registrant\n- `DELETE /users/{userId}` - delete/deactivate a user (account admin; may need `action` query param)\n- `DELETE /meetings/{meetingId}/recordings` - delete cloud recordings\n\nZoom returns `204 No Content` on success (empty body). This is a destructive, irreversible operation — confirm the target before calling.\n\nAPI reference: https://developers.zoom.us/docs/api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "zoom_get",
    "description": "Read any Zoom data via the Zoom REST API v2. Returns TOON format by default (30-60% fewer tokens than JSON).\n\nAuthenticates with Server-to-Server OAuth; the server mints and auto-renews the bearer token, so no per-call auth is needed.\n\n**IMPORTANT - Cost Optimization:**\n- ALWAYS use `jq` to filter response fields. Unfiltered responses are expensive.\n- Use `page_size` query param to cap result count (e.g., `page_size: \"30\"`, max 300).\n- To explore a schema, fetch ONE item with `page_size: \"1\"` and NO jq, then add a jq filter on later calls.\n\n**Checking the schedule / listing / \"search\":** Zoom has no single search tool — listing and searching are just GETs against the right path:\n- `/users/me/meetings` - your scheduled/upcoming meetings (use `type` query param: `scheduled`, `upcoming`, `upcoming_meetings`, `previous_meetings`)\n- `/users/{userId}/meetings` - another user's meetings (host must be in your account)\n- `/meetings/{meetingId}` - a single meeting's details (incl. `join_url`)\n- `/users` - list account users (`status`, `role_id`, `page_size`, `next_page_token`)\n- `/users/me` - the authenticated user\n- `/contacts?search_key={query}` - search contacts (Contacts scope required)\n- `/users/me/recordings` - cloud recordings (`from`/`to` date params)\n- `/report/users/{userId}/meetings` - meeting reports (`from`/`to`)\n\n**Pagination:** Zoom uses `page_size` + `next_page_token` (token-based) for most list endpoints. Pass the returned `next_page_token` as a query param to fetch the next page.\n\n**Output format:** TOON (default) or JSON (`outputFormat: \"json\"`).\n\n**JQ examples:** `meetings[*].{id: id, topic: topic, start: start_time, join: join_url}`, `meetings[*].topic`, `users[*].email`\n\nAPI reference: https://developers.zoom.us/docs/api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".\nExamples: \"/workspaces\", \"/repositories/{workspace}/{repo_slug}\",\n\"/repositories/{workspace}/{repo_slug}/pullrequests/{id}\"",
          "type": "string"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.\nExamples: {\"pagelen\": \"25\", \"page\": \"2\", \"q\": \"state=\\\"OPEN\\\"\",\n\"fields\": \"values.title,values.state\"}",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response. IMPORTANT:\nalways use this to extract only needed fields and reduce token costs.\nExamples: \"values[*].{name: name, slug: slug}\",\n\"values[0]\", \"values[*].name\". See https://jmespath.org",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default, 30-60% fewer tokens) or \"json\".\nTOON is optimized for LLMs with tabular arrays and minimal syntax.",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": true,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  },
  {
    "name": "zoom_patch",
    "description": "Partially update Zoom resources via PATCH to the Zoom REST API v2. Pass only the fields to change in `body`.\n\nAuthenticates with Server-to-Server OAuth (auto-renewed bearer).\n\n**Update (reschedule) a meeting:** `PATCH /meetings/{meetingId}`\n```json\n{ \"start_time\": \"2026-06-03T16:00:00Z\", \"duration\": 45, \"topic\": \"Project sync (moved)\" }\n```\n- Only the supplied fields change. Use `occurrence_id` as a query param to edit a single occurrence of a recurring meeting.\n\n**Other common patches:**\n- `PATCH /webinars/{webinarId}` - update a webinar\n- `PATCH /users/{userId}` - update user profile fields\n\nZoom returns `204 No Content` on success (empty body).\n\nAPI reference: https://developers.zoom.us/docs/api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "zoom_post",
    "description": "Create Zoom resources via POST to the Zoom REST API v2. Pass the JSON request body in `body`.\n\nAuthenticates with Server-to-Server OAuth (auto-renewed bearer).\n\n**Create a meeting:** `POST /users/me/meetings`\n```json\n{\n  \"topic\": \"Project sync\",\n  \"type\": 2,\n  \"start_time\": \"2026-06-02T15:00:00Z\",\n  \"duration\": 30,\n  \"timezone\": \"UTC\",\n  \"settings\": { \"join_before_host\": true, \"waiting_room\": false }\n}\n```\n- `type`: 1 = instant, 2 = scheduled, 3 = recurring (no fixed time), 8 = recurring (fixed time).\n- `start_time` is ISO-8601. Provide `timezone` for local times.\n- The response includes `id`, `join_url`, and `start_url`. **Starting a meeting is not an API action** — the host opens the returned `start_url` (it launches the Zoom client). Hand that URL to the user; the API cannot press \"start\" for them.\n\n**Other common creates:**\n- `POST /meetings/{meetingId}/registrants` - add a registrant\n- `POST /users/me/meetings/{meetingId}/invite_links` - generate join links\n- `POST /users` - create a user (account admin)\n\n**Tip:** filter the response with `jq` (e.g. `{id: id, join: join_url, start: start_url}`) to avoid returning the full payload.\n\nAPI reference: https://developers.zoom.us/docs/api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": true
    }
  },
  {
    "name": "zoom_put",
    "description": "Replace/update Zoom resources via PUT to the Zoom REST API v2. Pass the JSON request body in `body`.\n\nAuthenticates with Server-to-Server OAuth (auto-renewed bearer).\n\nPUT is idempotent — used where Zoom expects a full-representation update. Most meeting edits use PATCH (see `zoom_patch`); PUT is used for settings-style endpoints, e.g.:\n- `PUT /users/{userId}/settings` - update a user's settings\n- `PUT /meetings/{meetingId}/status` - end/recover a meeting (`{\"action\": \"end\"}`)\n- `PUT /meetings/{meetingId}/recordings/status` - recover recordings\n\nZoom write endpoints typically return `204 No Content` on success (empty body).\n\nAPI reference: https://developers.zoom.us/docs/api/\n",
    "inputSchema": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "$defs": {
        "OutputFormatArg": {
          "description": "Serializable/deserializable `OutputFormat` for tool arg surfaces.",
          "type": "string",
          "enum": [
            "toon",
            "json"
          ]
        }
      },
      "required": [
        "path",
        "body"
      ],
      "type": "object",
      "properties": {
        "path": {
          "description": "The Bitbucket API endpoint path (without base URL). Must start with \"/\".",
          "type": "string"
        },
        "body": {
          "description": "Request body as a JSON object. Structure depends on the endpoint.\nExample for PR:\n`{\"title\": \"My PR\", \"source\": {\"branch\": {\"name\": \"feature\"}}}`"
        },
        "queryParams": {
          "description": "Optional query parameters as key-value pairs.",
          "type": [
            "object",
            "null"
          ],
          "additionalProperties": {
            "type": "string"
          }
        },
        "jq": {
          "description": "JMESPath expression to filter/transform the response.",
          "type": [
            "string",
            "null"
          ]
        },
        "outputFormat": {
          "description": "Output format: \"toon\" (default) or \"json\".",
          "anyOf": [
            {
              "$ref": "#/$defs/OutputFormatArg"
            },
            {
              "type": "null"
            }
          ]
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": true
    }
  }
]
